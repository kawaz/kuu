{
  "why": "help_after の循環は DR-113 §8.2 により definition-error kind=circular-ref となる。非循環連鎖と不在 target fallback は受理されるため、相互循環だけを静的 reject として分離する。",
  "query": "definition_error",
  "definition": {"options": [
    {"name": "a", "type": "flag", "long": true, "help_after": "b"},
    {"name": "b", "type": "flag", "long": true, "help_after": "a"}
  ]},
  "cases": [{
    "id": "mutual-after-is-circular-ref",
    "why": "DR-113 §8.2 により a after b と b after a は同時に満たせず、循環の全参加要素 a, b を circular-ref として報告する。",
    "expect": {"outcome": "definition-error", "errors": [
      {"element": "a", "kind": "circular-ref"},
      {"element": "b", "kind": "circular-ref"}
    ]}
  }]
}
