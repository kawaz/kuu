{
  "why": "variant DSL の全入口を同一 option entry の spellings に素材化する。spellings は定義出現順で long 入口を並べ、その後に short 入口を併記する。",
  "query": "help",
  "definition": {"options": [
    {"name": "ssl", "type": "flag", "long": [":set", "no:set:false"], "short": "s", "help": "enable TLS"}
  ]},
  "cases": [{
    "id": "variant-entries-and-short-listed-in-order",
    "why": "DR-113 §4.4 の spellings 素材として long 主入口 --ssl、variant --no-ssl を定義出現順で列挙し、その後に short -s を併記する。effect 自体は model に載せない。",
    "expect": {"outcome": "help", "command_path": [], "usage": {"has_options": true}, "options": [
      {"spellings": ["--ssl", "--no-ssl", "-s"], "help": "enable TLS", "hidden": false, "deprecated": false, "origin": "local"}
    ]}
  }]
}
