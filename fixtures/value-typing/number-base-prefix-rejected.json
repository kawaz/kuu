{
  "why": "canonical では基数 prefix と hex float を両方拒否 (DR-074 §1・§2): canonical (number_allow_base_prefix default false) の number は整数 hex `0x1F` も hex float `0x1p4` も受理しない → not_a_number Error。「16 進整数は書けるが hex float は Error」あるいは逆のねじれを作らないため両者は単一フラグに統合され、無効時は対称に両方 Error。opt-in で両方成功に転じる対照は number-base-prefix-optin.json。anchored 契約 (DR-074 §5) により `0x1F` を `0` に prefix 消費して残り `x1F` を捨てることもしない (Model B 非採用) — token 全体一致で全体が number でなければ Error。",
  "query": "parse",
  "definition": {
    "positionals": [
      {"name": "v", "type": "number"}
    ]
  },
  "cases": [
    {
      "id": "hex-int-rejected",
      "why": "`hex-int-rejected` は値 resident または piece filter が入力を値空間外と判定する断面。DR-037 Reject として枝は静かに脱落し、完全経路が無いため outcome=failure になるが、Reject の診断は errors と failure-action 候補へ保持しない。",
      "args": ["0x1F"],
      "expect": {
        "outcome": "failure"
      }
    },
    {
      "id": "hex-float-rejected",
      "why": "`hex-float-rejected` は値 resident または piece filter が入力を値空間外と判定する断面。DR-037 Reject として枝は静かに脱落し、完全経路が無いため outcome=failure になるが、Reject の診断は errors と failure-action 候補へ保持しない。",
      "args": ["0x1p4"],
      "expect": {
        "outcome": "failure"
      }
    }
  ]
}
