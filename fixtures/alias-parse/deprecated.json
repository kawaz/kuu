{
  "why": "deprecated alias (DR-058 §2 + DR-057): deprecated:true が付いた alias 入口は受理不変 (canonical セルへ普通に束縛) だが、その入口で起動されると ParserContext の warnings に構造化警告 {element, kind:\"deprecated\"} を積む。canonical `port` (-p) + 通常 alias -n + deprecated alias -o の 3 入口はすべて port セルへ合流する (link 同型) が、警告を積むのは deprecated 入口 -o のみ。警告の element は canonical (port) — 「use <canonical> instead」の自動導出素材 (DR-058 §2、DR-057 §2)。deprecated 値は v1 では bool のみ。警告の表示・文言はレンダラの関心で仕様外 (DR-058: 素材はフィールド、文言はレンダラ)。DR-021 の filter warn (パース中警告) とは別層 (パース成功後の利用推奨警告)。warnings は CONFORMANCE §2 success の optional フィールドとして定義済み — 各要素 `{element, kind}`、element は canonical セル参照 (DR-058 §2)、kind は機械可読識別子 (v1 は \"deprecated\")、ParserContext (DR-016) の warnings — DR-058 §2 による拡張 — の projection。sources と同じ optional 検証で、element は集合比較・kind は fixture に書かれた要素のみ比較する (CONFORMANCE §3)。蒸留元: slice phase26 test 7。",
  "query": "parse",
  "definition": {
    "options": [
      {"name": "port", "type": "number", "short": "p"},
      {"alias": "port", "short": "n"},
      {"alias": "port", "short": "o", "deprecated": true}
    ]
  },
  "cases": [
    {
      "id": "deprecated-entry-warns",
      "why": "deprecated 入口 -o: 受理は不変で port=80 へ束縛 (effect entity port)、かつ deprecation 警告を積む。警告 element は canonical port (どの入口が deprecated かでなく『代替すべき canonical』を指す、DR-058 §2)。受理と警告が両立する (deprecated は reject でなく warn) ことの固定。",
      "args": ["-o", "80"],
      "expect": {
        "outcome": "success",
        "effects": [
          {"entity": "port", "op": "set", "operand": 80, "source": "cli"}
        ],
        "result": {"port": 80},
        "warnings": [{"element": "port", "kind": "deprecated"}]
      }
    },
    {
      "id": "alias-no-warn",
      "why": "非 deprecated alias -n: 同じ port セルへ束縛するが警告は積まれない (warnings 空)。deprecated 属性が入口限定であること (-o だけが警告源、-n は無警告) の固定 (DR-057 §2 入口限定 deprecated)。",
      "args": ["-n", "80"],
      "expect": {
        "outcome": "success",
        "effects": [
          {"entity": "port", "op": "set", "operand": 80, "source": "cli"}
        ],
        "result": {"port": 80},
        "warnings": []
      }
    },
    {
      "id": "canonical-no-warn",
      "why": "canonical 入口 -p: 当然無警告 (warnings 空)。-o (deprecated, 警告あり) との対照で『警告は deprecated 入口の発火にのみ紐付く』ことを固定する。",
      "args": ["-p", "80"],
      "expect": {
        "outcome": "success",
        "effects": [
          {"entity": "port", "op": "set", "operand": 80, "source": "cli"}
        ],
        "result": {"port": 80},
        "warnings": []
      }
    },
    {
      "id": "deprecated-then-canonical-mixed-still-warns-once",
      "why": "混在起動での入口限定の最直接検証 (issue deprecated-warning-boundary-fixtures (1)、規定は DR-057 §2 / DR-058 §2 の既存): deprecated 入口 -o と canonical 入口 -p を同一 parse 内で両方起動しても、warnings は -o の発火分 {element: port, kind: deprecated} 1 件のみ。deprecated 状態が canonical セル (port) に貼り付いて canonical/通常入口の発火まで警告源になる誤実装 — 『セルが deprecated』と『入口が deprecated』の混同 — を潰す。element が canonical 参照 (port) である規約と、警告発生源が入口である規約が独立であることの固定。",
      "args": ["-o", "80", "-p", "81"],
      "expect": {
        "outcome": "success",
        "effects": [
          {"entity": "port", "op": "set", "operand": 80, "source": "cli"},
          {"entity": "port", "op": "set", "operand": 81, "source": "cli"}
        ],
        "result": {"port": 81},
        "warnings": [{"element": "port", "kind": "deprecated"}]
      }
    },
    {
      "id": "deprecated-entry-repeated-warns-once",
      "why": "cardinality の畳み (DEP-Q1=a、kawaz 裁定 2026-07-17、DR-058 §2 明確化 note): 同一 deprecated 入口 -o を 1 parse 内で 2 回起動しても、warnings には {element: port, kind: deprecated} を 1 件だけ積む (element 単位の重複排除)。warnings の用途は『use <canonical> instead』の導出素材であり発火回数は素材に不要 — 回数が要る場面はレンダラが別素材で扱う関心。effects は 2 発火とも記録される (scalar は last-wins で result は 2 発目の 81) — 発火自体の記録 (effects) と利用推奨警告 (warnings) の cardinality が独立であることも本 case が固定する。",
      "args": ["-o", "80", "-o", "81"],
      "expect": {
        "outcome": "success",
        "effects": [
          {"entity": "port", "op": "set", "operand": 80, "source": "cli"},
          {"entity": "port", "op": "set", "operand": 81, "source": "cli"}
        ],
        "result": {"port": 81},
        "warnings": [{"element": "port", "kind": "deprecated"}]
      }
    },
    {
      "id": "deprecated-entry-then-failure-drops-warnings",
      "why": "failure 時の warnings 破棄 (DEP-Q2=a、kawaz 裁定 2026-07-17、DR-058 §2 明確化 note): deprecated 入口 -o の起動が成立した後、後続の未定義トリガ -z で parse が失敗すると、failure outcome に warnings は載らない (内部で積んだ警告は最終エラー時に捨てる — deprecated 警告は『パース成功後の利用推奨』であり、失敗した実行に乗り換え推奨を出しても行為として成立しない)。lint / 診断モードなら error も warning も全部出すべきだが、それは別層の関心 (DR-047 §7 系譜) で本 wire の対象外。errors は -z (pos 2) の未定義トリガ = 無帰属 unexpected_token (fixtures/failure-actions/args-minimal.json と同形、element なし)。",
      "args": ["-o", "80", "-z"],
      "expect": {
        "outcome": "failure",
        "errors": [
          {"args_pos": 2, "kind": "parse", "reason": "unexpected_token"}
        ]
      }
    }
  ]
}
