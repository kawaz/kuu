# DR Index

引数定義 AST 設計の決定記録 (Design Record) 一覧。各 DR の Status / Superseded 関係は本体ファイル末尾を参照。

## パース意味論

- [DR-021](DR-021-longest-match-and-ambiguous.md): 露出キー一意性検査は実行時、静的バリデータは warn のみ — パース成功条件は updated by DR-038 (完全経路一意性に再定義)、露出キー一意性検査は superseded by DR-120 (definition-error へ)
- [DR-037](DR-037-filter-reject-error-and-branch-resolution.md): filter の Reject/Error 区別、解けた枝の数による結末分類
- [DR-038](DR-038-parse-semantics-path-uniqueness.md): パース意味論の確定 — 「完全経路の一意性」を契約に、長い経路を優先する規則は持たない、実装契約は bounded path-search
- [DR-041](DR-041-token-reading-semantics.md): トークン読みの意味論 — 読みは枝 (多重 Accept)、greedy は面で優先 (先食い)、prefix ガード非採用、dashdash は再スコープ化
- [DR-048](DR-048-failure-time-action.md): 失敗時アクション — early-exit は持たない (完走後の表示選択)、汎用属性 + type:help 同梱、衝突は argv 位置の先勝ち、候補経路は dead end 込み、ambiguous では非発火 (誘導行で補完)
- [DR-053](DR-053-parse-outcome-structure.md): パース結末の構造 — outcome 3 値 discriminated union、errors 全保持 + primary は argv 最深、ambiguous は全解釈列挙 (結果オブジェクト形)、help_entry / tried_triggers はフィールドで文言はレンダラ
- [DR-054](DR-054-parse-definition-failure.md): parse_definition の失敗挙動 — 構成不能/全入力破壊 = Error・一部入力の驚き = warn の境界基準、v1 Error は構文/値域/参照検査まで、定義エラーは全列挙 + hint
- [DR-073](DR-073-export-key-collision-carrier.md): export-key 衝突の担体 — ambiguous 維持 + 解釈ごとの optional claimants 面 (露出キー → 占有実体 entity)、識別子は entity (値/source では区別不能)、fixture は {result, claimants} の組で集合比較 (順序非依存)、独立 outcome / 値オブジェクト化 / failure 化を不採用、lint は別綴り co-export に link 提案 — **superseded by DR-120** (衝突が definition-error になり claimants を生む outcome が消滅、link 提案は error hint へ吸収)
- [DR-097](DR-097-greedy-reading-viability.md): 先食い・早閉じ抑制の「読める」の精密化 — DR-041 §4 の述語はトリガ一致でなく読みの成立 (その greedy 読みを経由する完全経路の存在) を指す、成立しない読みは読みゼロと同じ扱いで素通し枝が生き他ルートが通れば held Error は捨てられる (DR-037 適用)、早閉じ抑制にも同じ述語を共有し及ぶ、typo マスク受容は DR-041 §5 の既定トレードオフの一貫適用

## 2層 AST / 構造プリミティブ

- [DR-001](DR-001-two-layer-ast.md): 2層 AST 構造 (UsefulAST / AtomicAST)
- [DR-002](DR-002-element-isomorphism.md): 全要素は同型、CLI 慣習名はシュガー — 適用範囲は updated by DR-017 (AtomicAST 限定)
- [DR-017](DR-017-command-first-class-at-definition.md): command は定義時1級、パース時同型
- [DR-018](DR-018-placement-and-commands-sugar.md): 配置で区別、commands は positionals 内 or 糖衣
- [DR-019](DR-019-repeat-merged-into-multiple.md): repeat を multiple に統合、可変長 positional — multiple の内部構造は reorganized by DR-034、repeat との統合は DR-043 で分離
- [DR-020](DR-020-recursion-via-primitives.md): 復帰/途中分岐は専用概念を持たず構造で組む
- [DR-023](DR-023-structural-primitives-finalized.md): 構造プリミティブ確定形 (4 + multiple + 糖衣)
- [DR-026](DR-026-leaf-branch-and-sugar.md): 葉/枝、exact は値プリミティブの一種、構造記法の糖衣 (裸文字列=exact, 裸配列=seq)
- [DR-027](DR-027-serial-renamed-seq.md): serial → seq 改名 (or/seq/multiple = alternation/concatenation/closure)
- [DR-039](DR-039-atomicast-convergence-and-vertical-slice.md): AtomicAST = ボトムアップエンジンのシリアライズ形、垂直スライスで実装と共設計、JSON Schema は最後 — 直列形の範囲は DR-063 で確定

## 名前とスコープ

- [DR-003](DR-003-name-three-axes.md): name は3軸 (CLI起動/結果key/内部参照) を兼任
- [DR-006](DR-006-scope-and-lexical-resolution.md): スコープは自動、lexical scope chain で解決 — updated by DR-033 (lexical スコープ = 結果スコープに統一) / TRG-Q4=a (スコープ生成条件は解決後の結果キー軸の占有) / DR-136 §6 (各スコープでの照合は raw 一致優先の 2 段ルックアップ)
- [DR-022](DR-022-snake-case-naming.md): キー名 snake_case、case 変換 pluggable
- [DR-024](DR-024-three-name-layers.md): 名前は3層 (key name / def name / value_name)
- [DR-025](DR-025-name-creates-scope.md): 結果スコープを作る条件と露出規則 (最も浅い層で止める) — **現行の生成条件は解決後の結果キー軸 (`export_key`) の占有** (TRG-Q4=a で「name の presence」から訂正。`export_key: null` は透過、name 無し + 明示 export_key はスコープを作る)
- [DR-033](DR-033-lexical-scope-equals-name-scope.md): lexical スコープ = 結果スコープ (同一単位、command に限らない) — 単位を決める条件は TRG-Q4=a で結果キー軸の占有へ訂正。露出が透ける段は名前も透ける
- [DR-046](DR-046-name-axes-decomposition.md): name の軸分解 — id / 結果キー / value_name / display_name の目的別軸、name はデフォルト供給源 (nameless への ref/link が可能に)
- [DR-052](DR-052-export-key-unification.md): 結果キー軸の一本化 — export_key: string | null (export bool 廃止)、null = nameless 同化の透過 (値は流れる)、選ばれた name スコープは空でも `{}`
- [DR-120](DR-120-export-key-single-cell.md): 露出キーに対応する値セルはちょうど 1 つ (EXK-Q1〜Q4) — 1 結果スコープで同一露出キーへ解決する別セルが 2 つ以上ある定義は definition-error `export-key-collision` (DR-021 の実行時検査 / DR-073 の claimants 担体を置換)。判定は export_key 適用後の解決キー文字列で行い identity / mapped を区別しない、スコープ生成要素 (command 含む) も結果キーを占有する要素として参加、link / alias 参照ノード・dd・export_key null は非参加。検査は構造的で経路の到達可能性を見ない (or の別枝・排他 command・exclusive_group 兄弟も対象)。正当な用途は link (入口 N : セル 1) と or 席 (セル 1 : 枝 N) で書け、綴り軸の重複は従来どおり warn + 実行時 ambiguous のまま。hint は衝突要素の値空間で出し分ける (一致 → link / 不一致 → or) — 判定は type プリセット展開後の値セルの型で綴りでは見ない (count と number は一致 = DR-029 の canonical link 例)、規範化するのは提示する手段までで文言はレンダラの関心 (DR-082 §1 で hint は非比較)
- [DR-136](DR-136-trigger-name-axis.md): 綴り軸 `trigger_name` の一級化と name の名前系値源への純化 — CLI 表面の照合綴り (long の基幹綴り / command のトリガ / alias の入口綴り) を担う軸を独立させ、name 直取り (DR-071 §3 / command の name 焼き込み) を置換。name は各軸へのデフォルト供給源に徹し、供給時のみ**文字単位 1:1 の写像**が掛かる (ASCII 英数と非 ASCII は保持し、ASCII 範囲の非英数だけを trigger_name は `-` へ / id・export_key は `_` へ置換。value_name はさらに ASCII 大文字化、display_name は raw name)。camel 系変換は含まず、明示指定には一切掛からない。name はハイフンも許容され `dry_run` と `dry-run` は全軸で同一に落ちる。positional (入口綴りなし) / short (明示専用) / exact・values (literal 直値) は綴り軸に不参加、alias は綴り軸の別名を足す入口ノードとして再解釈 (DR-057 §3 の再導出を trigger_name 語彙へ)

## 配置 / options / positionals / commands

- [DR-004](DR-004-options-positionals-split.md): options / positionals の2分割、commands は or でラップ
- [DR-030](DR-030-entity-only-node.md): 実体だけノード (入口属性なしの値ノード、appconfig ストア用途)
- [DR-134](DR-134-command-value-or-scope.md): command は値かスコープのどちらかを名乗る (CNV-Q1=b / CNV-Q1b、kawaz 2026-08-12 / **CVQ-Q1a で §1 明確化 2026-08-14**: §1 の「フィールド名 + JSON scalar / array」は「その座の値が map (スコープ) かそうでない値か」の対比表現であって配列 literal の担体を約束した文ではない — **担体は scalar literal のみで、配列 `value` / 配列 `default` は definition-error `invalid-range`**。非 multiple スカラー要素への配列 default を静的に倒す既存の線 (DR-083 §5) と同族で、§5「値の供給は既存の node 意味論のまま」の帰結。配列 value の合法化 (CVQ-Q1b) は accum 化が multiple 未宣言セルへ反復系規則を持ち込み、配列 literal 許容が DR-083 §5 と食い違うため不採用) — command node の `value` は合法 (スコープ生成は command 特有でなく named or / seq も作る、スコープ = その座の値が map というだけで、親から見ればどちらもフィールド名 + JSON 値)。**選択された値持ち command は親スコープのキーに値そのものを座らせる** (kv ではない、sources は値を確定させた席のタグで `value:` 由来なら `const`)。**値持ち command の内側に結果キー占有子 (DR-120 §4 占有側) を置くのは definition-error kind=invalid-range** — 1 露出キー = 1 値セル (DR-120 §1) で 1 セルが持てる値は 1 つ、占有子が居れば射影は宣言キー全列挙の kv に確定し (DR-130 §1) value と席を争うため。kind は「その席に置けない属性の組合せ」の既存 kind からの類推 (default × default_fn 併用等と同族)。非占有子 (`link` / `alias` / `dd` / `type:"none"` / `#` 内部セル / 占有する子を持たない透過ノード) は共存可。未選択なら値持ち・`default` 持ちでも `null` で一様 (DR-130 §2、未選択 or 枝の default が漏れない既存規範と同型)、sources も同座で null。値供給は既存 node 意味論のまま (`value:` / `default` / `default_fn` / cell fn) で command 専用規則は無く、露出キーの占有と衝突検査も不変 (どちらを名乗っても 1 キー 1 セル)。**schema 変更なし** (`commands` の要素は `#/$defs/node` 再帰で `value` は node 共通属性 — spec 側は元から合法で、乖離していたのは参照実装)。CNV-Q1a (command の value を definition-error) / 予約キーへの value 逃がし / 値と kv のマージを不採用
- [DR-064](DR-064-dd-declaration-placement.md): dd の宣言配置 — canonical は options[] (順不同・greedy 面住人の分類一致、dd = 効果が severed 化の特殊 flag)、配置は挙動不問、usage の [--] 表示はレンダラの慣習 (宣言と表示の分離)

## 値と型 / type 参照糖衣

- [DR-005](DR-005-type-categories.md): type の3カテゴリと子からの値型推論
- [DR-015](DR-015-value-propagation.md): 値の発生と伝搬の構造的セマンティクス
- [DR-028](DR-028-type-as-reference.md): type は definitions/registry への参照糖衣、解決順、前方互換、flag等は糖衣プリセット
- [DR-032](DR-032-ref-link-name-resolution.md): ref/link が指すのは name (解決はスコープ内→definitions)、type とは別物
- [DR-040](DR-040-type-registry-dialects-and-restriction.md): type registry の方言運用 (canonical default / 言語DX / ユーザ差し替えの3層上書き、寛容default+pre_filter vs value_parser 差し替えの2軸) — canonical 字句仕様 (数値・exact codepoint 比較・path バイト列・count_or_set・filters 3 層) を拡張確定。number 字句は updated by DR-074 (10 進最小 → 実用寛容固定字句)、bool 字句は DR-074 で新設
- [DR-074](DR-074-canonical-number-bool-lexicon.md): number / bool canonical 字句の確定 — number は実用寛容な 10 進固定字句 (leading `+` / `007`=decimal / `.5`・`1.` / `_` default 桁区切り / inf は float 型のみ・nan は両型 Error / 型 suffix 非採用)、基数 prefix + hex float は `number_allow_base_prefix` 統合 opt-in (canonical では `0x1F` も `0x1.8p3` も Error)、bool は `["true","1"]`/`["false","0",""]`+ci (yes/no は opt-in)、bool↔number は文字列 parse 可・型変換 Error、anchored 契約明文化、負数 arity 駆動消費、G8 (最寛容 vs 最小) 解消。JSON 同型 / 型 suffix / nan 受理 / 個別 base 指定 / empty_is_false / 読みモードを不採用。int の値空間判定 (§2 暫定注記の M2) は confirmed by DR-075
- [DR-075](DR-075-int-rounding-modes.md): int の値空間判定 (M2 確定) と int_round (小数入力の丸めモード) — int は整数「値」を受理 (`"3.0"`→3 / `"1e3"`→1000、fractional のみ int_round に従う、DR-074 §2 の読みを採用し DR-050 §4 の構文判定を supersede)、`kuu_int_parser` factory の config キー `int_round` は 10 種体系完備 (方向 4 `floor`/`ceil`/`trunc`/`away` × {非 half, half} + `half_even` + `error`)、canonical default `error`、命名は kuu 独自一貫系 (HALF_UP/HALF_DOWN 罠を回避し `away`/`trunc` で 0 起点向き・`ceil`/`floor` で無限起点向き)、String 源は binary64 非経由の厳密判定を必須要件化 (`parseFloat`→round は不適合、native-number 源は JSON 由来 binary64 で対象外)、`not_an_integer` は error モードのみ emit。CLI パーサ界に前例なし。構文判定維持 (選択肢 Q) / half_05up / pre_filter 退避のみ / HALF_UP 系命名を不採用
- [DR-092](DR-092-inf-float-json-serialize.md): inf / 非整数 number の JSON serialize 規約 — inf は値位置に文字列 sentinel `"Infinity"`/`"-Infinity"` (protobuf3 canonical JSON 同型、先頭 `+` 付けない)、parse-serialize 対称性は parse 側の寛容 (DR-074 §7 の `Infinity` case variant 受理) が担う非対称採用、適用範囲は result/effects/config 由来値の一律、非整数 float の canonical serialize は shortest round-trip (Ryu/Grisu 系)、precision-critical 値は fixture 直接 pin を避け opt-in 曖昧さ回避は方向付けのみ、config JSON 値位置への `"Infinity"` 供給は DR-050 §4 既存経路で自動成立 (確認的明記)。タグ付き object / JSON5 bare literal / 小文字 `inf` canonical / 入力 case 保存を不採用
- [DR-076](DR-076-bool-is-value-type-flag-is-sugar.md): bool は通常の値型 (variant 持ちでも eq-split 値形を受ける)、flag が特殊 — 特殊性は flag 側の展開 (long 綴り合成: long:true 糖衣差し替え + 非空リスト補完) で吸収。presence-only へ寄せる案・flag 廃止・node 語彙正規形を不採用
- [DR-077](DR-077-update-effect-and-count-normal-form.md): 効果 op に update を追加 (old へ filters registry の T=>T transform を適用する 0-token 効果)、count = number + default:0 + long:true 糖衣 [":update:increment"] の綴り合成に正規化。multiple increment accumulator は count から退役
- [DR-078](DR-078-definitions-templates-and-ref-identity.md): definitions に templates 区分を新設 (クロージャなしの共有消費文法 Node の座席、ref の解決先)。ref/link/alias の参照実体は name lookup で束縛される内部 id (DR-032 の精密化)。types 相乗り・頂上 templates 節を不採用
- [DR-051](DR-051-absent-result-semantics.md): 結果の欠落表現 — 値の無い要素は absent (キーなし)、null は値空間に持たない (config null = 供給なし)、型導出は required/default/反復系 → T・それ以外 → T? — **superseded by DR-130** (射影が反転し、値の無い座は null になる)
- [DR-130](DR-130-null-result-projection.md): 結果射影は宣言キーの全列挙、値の無い座は null (kawaz 提案 + NUL-Q1〜Q4 / NUL-C1 全確定 2026-08-01、**DR-051 を全面 supersede**) — 「値の無さは構造 (キーの不在) で語る」から「値の無さは値 (null) で語り、構造は宣言どおり常に立つ」への反転。成功 result の各実現スコープは宣言上出うる全キーを必ず持ち、埋まらない座は `null`。**未選択の子 command scope は親レベルに `subcmd: null` と出るだけで再帰は 1 段で止まる** (null がサブツリー展開ごと置き換えるので、result の大きさは実現した経路の宣言幅に比例)。**null は値空間の住人へ昇格**し、パイプラインは **null 素通しの 1 規則** (filter / value_parser / pieceProcessor は null に触れず通す、Option monad の map と同型) で畳めるため DR-051 が懸念した全域 nullable 化は起きない。これにより**配列の穴が値として表現可能**になり (nameless tuple の部分書きは `[null, 2]`)、LNK2-Q1=a (全座が埋まるまで tuple ごと absent) は obsolete (全座成立判定は転用、挙動を null 埋めへ差し替え)。全列挙の対象は**静的に宣言キーが分かる構造**に限る (NUL-Q3): or はセル単位で 1 キー (unset なら `cell: null`、枝は同時列挙しない = DR-120 §2 の帰結) / repeat 行の内側も null 埋め / **record (DR-126) の内側も反転** (closed 語彙なので全フィールド列挙 + null、DR-126 §3 の presence-optional を改定) / **動的キー構造 (from_entries・merge・kv-map・config 由来 map) は present のみ**。**sources も null** (NUL-Q4 — result と同型のキー集合を維持、null 座 = 確定主体なし、DR-122 の shadow tree 規範は不変)。**presence marker (DR-052 §3 の空 kv) は概念ごと廃止** (全列挙で空になりようがなく、選択の有無は `{...}` vs `null` が語る)、`export_key: null` は結果キー軸のメタで**別軸のまま残置**。型導出は `T` / **`T | null`** の 2 分岐 (record 内側は全フィールド `T | null`)。**fixture は null 座も逐語で全キーを書く** (NUL-Q1=b、runner 自動補完は不採用 — 全列挙は case 比較がしやすく、runner が宣言から期待値を組むと fixture の実装独立性が崩れる) ため **CONFORMANCE §3 の比較規約を改定** (`result`/`sources` はキー集合込みの完全一致、「省略 = default 値と等価」の一般規約から除外。`candidates[].meta` の COMP-Q2 と同じ整理)。DR-051 が null を蹴った 3 理由への回答 = 二重表現は absent を**足すのでなく置き換える**ので生じない / 全域 nullable は素通し規則 1 本 / unset はセルのメタ状態であって射影の形ではなく、null が「値としての無い」を担うことで Sentinel 統一 (DR-131) の道が開く。**「宣言上出うるキー」の導出面は DR-120 の既存規定の合成** (§1b: 全 installer の宣言層寄与適用後の面 = DR-120 §5、占有/非占有は DR-120 §4 — 内部セル・`type:"none"`・`dd`・link/alias 参照ノード・`export_key:null` 透過は非占有、scope 生成要素は 1 キー) で新しい判定を足さず、`build_result` と `source_shadow` が同一集合を読む。**値述語 (required / required_group / DR-088) では `null` = 不充足** (キーが立つことは充足の証拠にならない)。**record の全フィールド補形は射影層の仕事**で type パーサの生出力は書き換えず、**DR-126 §4 の乖離検査は生出力に対して行う** ((c) 行「宣言済みキーが値に存在しない」は「正常 — 射影で null 補形」へ根拠替え、(a)/(b) の Error は不変)。**定義側に null リテラルは書けない** (§3.1: `default: null` / `value: null` は definition-error `invalid-range` — 「値なし」の明示は席の省略と同義で、許すと「default あり → T」の導出が破れる。`default_fn` が実行時に `null` を返すのは合法で座も sources も null)。**`ambiguous` の interpretations は sparse 維持** (§4.2 — parse 相の差分ビューなので全キー列挙しない、そこに置く null は「まだ決めていない」で §1 の null と意味が違う)。**conformance decoder 要件 3 つ** (§8.1: missing key と explicit null を別状態で保持 / `result`・`sources` を「省略 = default」一般規約から明示除外 / `effects[].operand` は present-required)。**出力増は意図したトレードオフ** (規範は O(実現スコープの宣言幅の総和 + closed record 展開) の出力を要求するところまでで、サイズ上限も lazy serialization も実装裁量)。**named 枝 or は全枝列挙 (NULOR-Q1=a)** — 選択枝の内側で非選択枝を `null` にし、セル未発火なら外側を `null` に畳む。未選択枝の default は充填しない。**誤爆注意**: `absent-ref` (定義時の名前解決失敗) / `absent-path` / `fixtures/link-parse/absent-target.json` / wire 入力側 presence は「参照先が存在しない」の別概念で対象外 (機械置換禁止)。**`absent-source` (値不在側) はここに含まれず廃止される** — 参照先の座は不在にならず null になるので borrow は null を返し `set(null) = unset` が伝播する (DR-113 §5.4 の個別規定は DR-131 §1.1 へ吸収、NUL-C1 の「対象外」は誤爆置換防止の意図であって温存指示ではない)。**別軸の null** (§9.1): `export_key: null` (結果キー軸のメタ) と provider 境界の `| null` (`tty_provider` 等の「情報なし」= Maybe、kuu 値空間へ流入せず供給なしとしてラダー次段へ) は層が違う — 帰結として **result JSON は config JSON への round-trip 形ではない**。現行 absent 維持 (穴が開けられずスキーマ発見性も得られない) / 射影層限定 null (Sentinel 統一ができず位相の違う住人が増えるだけ) / runner 自動補完を不採用。波及は DR 部分改定 9 本 (DR-052 §3§4 / DR-113 §5.4 / DR-120 §4 / DR-121 §2 / DR-122 §2 / DR-123 §3 / DR-081 / DR-103 §5 根拠付け替え / DR-126 §3§4) + 注記 11 本 (DR-064 §5 / DR-089 は「値空間なし = 露出キーを持たない」で全列挙対象外)・docs 27 箇所 (DESIGN §2.6 と CONFORMANCE §2/§3 が本丸、DESIGN §12b の tty absent 落ちも null 化)・fixture 578 case の逐語全列挙化 + sources 217 case + why 78 ファイル/107 case (2026-08-01 実測、サイクル 3 で再集計) + **`fixtures/absent/` 4 ファイルは `fixtures/null-projection/` へ改称**して書き直し + `default-fn-borrow-ladder.json::borrow-source-absent` は更新対象へ・kuu.mbt の `build_result` / `source_shadow`

## multiple

- [DR-008](DR-008-multiple-field.md): multiple フィールドに複数値関連を統合 — 内部構造は reorganized by DR-034
- [DR-034](DR-034-multiple-structure.md): multiple の構造モデル (pieceProcessor/separator/accumulator/collector、縮退ケース、type と multiple は同じ属性平面) — mapper→accumulator の改称は updated by DR-036
- [DR-043](DR-043-repeat-and-multiple-split.md): repeat (構造閉包、min/max は枝生成に効く、ref 再帰へ lowering) と multiple (値の畳み) の分離、両者 installer 化 (DR-019 の統合を部分的に覆す)
- [DR-044](DR-044-repeated-group-result-shaping.md): 反復グループの結果整形 — 配列が既定、map は from_entries (entries 配列 / 指名 2 フィールド / key 昇格の 3 用法)
- [DR-080](DR-080-merge-accumulator.md): merge accumulator — piece マーカー語彙 (remove 全削除 / @ splice / + escape、全体一致のみ)、L→R 評価 (marker なし = 上書き、marker あり = merge + 暗黙先頭 @)、remove は双方向 (作業リストと後続 @ の splice 内容)、old = セル現在値 (初回は下位席勝者 = ラダー合成の限定口、unset は f=identity の縮退)
- [DR-105](DR-105-accumulator-flatten-and-array-filter-fallibility.md): accumulator `flatten` ダイヤル (append 専用、既定 false、true で発火値配列を 1 段展開) — 独立 accumulator エントリ `flatten` (DR-036/043) を統合廃止、他 accumulator への宣言は definition-error kind=invalid-range (merge×ref の DR-084 §3 と同型)。ARRAY filter registry (`accum_filters`) の fallibility 確立 (「filter 席=fallible/構造装置=total」の勢力図適用) + 最初の Validate 系住人 `length_range:min:max` 追加、DR-102 §4 の reject 位置帰属 (argv.length) が実効化

## ref / link / definitions

- [DR-007](DR-007-definitions-ref-link.md): definitions 領域、ref (構造継承) と link (値同期) — reorganized by DR-035 (definitions と registry の名前空間統一)
- [DR-029](DR-029-link-revisited.md): link 見直し (値同期、1実体:N参照、固定パス DSL、遅延解決、失敗=パース失敗)
- [DR-127](DR-127-link-fixed-path-dsl.md): link 固定パス DSL の実装可能化 (kawaz チャット裁定 2026-07-31、LINKPATH-Q1〜Q7 全確定) — **union セルの動的規定 (§2.2 union 行の実行時 Reject / §3 union 器 / §4 の union セル適用) は superseded by DR-138 §9、遷移表への tuple 行追加は DR-137** (単相セルの全規定は現役) — DR-029 が文法と「遅延解決」だけを持ち意味論を欠いていた穴を埋める。パスを**セル空間の接頭辞 + 値空間の残余**の 2 相に分解: 第 1 相は lexical → definitions で root を解き宣言構造で辿れる限りセル降下 (定義時静的、解決不能は `absent-ref`)、残余が空なら bare link と完全同一のセル同期 (ラダー・accumulator 無傷)。第 2 相は葉セルの値内部への降下で、**record 宣言 (DR-126) があればパス妥当性も定義時静的** (フィールドの type の out を辿る型依存グラフの降下)に昇格し実行時に残るのは値読みと presence 判定のみ。降下は **value_type 全域の遷移表**で決まる (record = 語彙も型も静的 / array = `[int]` の構造は静的だが index の存在は実行時 / map・value = 以降全て実行時 / primitive = 残余があれば恒真不成立で definition-error / union = パスを含む variant が 1 つ以上あれば合法、型不一致なら definition-error、実行時は現在値の構造で判定)。二相性は「スコープの kv はパース時に存在せず、不透明値の内部にセルは存在しない」という構造の写像であって発明した規則ではない。**部分書きは 2 層** — record なら器 `{}` を auto-vivify (`--until X` 単独で `{until: X}` が成立値、closed record ゆえ器の形が定義時に確定) / 宣言なしなら枝 Reject。vivify 値は value_parser を通らないのでフィールド横断 invariant は final_filters の領分、organic 値は静的パス検査 + set 時の**フィールドの type** の pieceProcessor で二重に保証 (パースを担うのは入口セルの type 宣言でなくフィールド側の type)。定義片 default と organic 部分書きは**無橋** (各経路が自分の既存規則、型導出は out.record 一本)。**入力側への部分注入はしない** (in 候補バッファは効果時系列にもラダーにも席が無く args_pos 帰属が壊れる) — sub-parse は入力世界、link path は出力世界。値残余の座への操作は set + Value 返し fn のみ (Sentinel 返しは発火時 Reject)、時系列適用が組み合わせを全て決める。観測面は sources = 座単位の構造分解 (Q4=a、部分書きした座だけ `link` タグ)、effects = `path` segment 配列の optional 追加 (Q5=a、結合文字列は非単射で不採用)。`path` は値残余ではなく**`entity` (最寄り named セル) から実着地座までの観測アドレス**であり、この 1 規則が値残余の着地と nameless 透過子への着地 (`pair[0]` = `entity:"pair", path:[0]`) を特例なしに覆う (省略 = 既定値 `[]` = セル丸ごと着地)。実装は**裁定前に枝ローカル効果列 fold でパス解決可否を判定**する必要がある (DR-038 の系)。DR-029 へ分界文を追補: 「name 参照は定義時に束縛、値構造の降下だけが遅延」。全遅延・値一元案は effects の cell 単位規範への逆写像が一意にならず棄却
- [DR-138](DR-138-union-branch-culling.md): union 値の枝並行構築と確定相淘汰 (kawaz 裁定 2026-08-14〜16、UC-Q1〜Q3、**DR-127 §2.2 union 行の実行時規定と §4 の union セル適用を supersede** — 範囲の正本は §9 逐条表、単相セルの全規定と既存 fixture は無傷) — union 宣言セルへの書き込みは**適合する全枝へ並行着地** (含まない枝には着地しないだけで Reject は発生しない)、確定相で**未完成枝を淘汰**し、生存枝のうち**最終着地が時系列で最も新しい枝**が勝つ (完成順ではない — 最後に触った枝がユーザの意図)。完成は型レベルの構造述語 (tuple = 全座 non-null / record = presence-optional なので部分値でも完成 / primitive・array・map・value = 型適合の着地 1 件以上) で、node `required` (できた最終セル値への値述語、DR-047) とは 2 層を混ぜない。**ラダーはセル単位で 1 回** (DR-031/120 不変) — CLI/link 書き込みが 1 件でもあれば下位席は発火せず生存ゼロでも env は救済しない、部分書きの機構は CLI/link 層にしか無いので「下位席が枝を完成させる」状況は構造的に発生しない。**生存ゼロは完全経路の不成立 (Error 保持)** — DR-047 §2 の経路フィルタと同じ席で事後検証層は増えず、他の読みが通れば捨てられる (DR-037)。kind は新設せず **`parse` / reason `incomplete_value`** (単相 tuple の null 座残り確定も同 reason — 同じ完成判定の 2 つの現れ)、args_pos は着地書き込みの argv 最深 operand 位置、message に union/branch 等の内部実装語を出さない文言ガイド付き。**書き込みゼロの union セルは淘汰対象外** — 通常ラダー → 全席空なら DR-130 §1 の null (未指定エラー化しない)。**書き込み単位の失敗は淘汰に吸わない** — operand 型不正 (`--r abc`) は書き込み時の通常 Reject/Error のまま (淘汰に遅らせてよいのは後続書きで治りうる座の充足だけ)。**tie は観測値 (射影後) が同値なら成功・異値なら ambiguous** (DR-031 EXP-Q1 の同型、順位規則で選ばない — 共有フィールドを持つ record variant への 1 書き込みは射影差 {a:1} vs {a:1,b:null} で ambiguous)。**effects はセル単位 1 entry で敗北枝のみ着地の書き込みも残り** (効果は観測記録、DR-045)、sources は勝ち枝の座のみ (DR-122)、経路同一性 (効果列基準、DR-038) は枝表現に依存しない。**消費の or とは別機構** (トークンの食い方を分岐 = 消費の or / 食い方が同じで値の型だけ分岐 = セル union。input_structure の or は消費側)、枝並行構築は出力側の候補集合で DR-127 §5 (入力側部分注入禁止) は無傷。conformance ビークルは `fixture/color` (tuple×string union) / `fixture/shape` (record 2 variant、tie 用) を追加し `fixtures/union-parse/` が pin。color の record 化 (vivify + 時系列の枠内) / 枝別ラダー / 型不正の淘汰吸収 / tie 無条件 ambiguous / 生存ゼロ新 kind を不採用
- [DR-139](DR-139-value-capsule-attribute-plane.md): 値カプセルの属性平面 — DR-034 の実体化 (VC-Q1=b + AP-Q3〜Q5 / UC-Q4 / CR-Q1〜Q3、kawaz 2026-08-16 mid=32-65) — 要素の「値がどう作られるか」は **1 個の値カプセル (canonical field 11 = 供給系 3 + 変換 5 + 収集 3)** に集約。**供給の canonical は `defaults` 配列** (default provider fn の試行列 — 順序 = 試行順、最初に成立した供給が勝つ = DR-081 等価。要素は colon-string fn (`env:X`/`config:p`/`borrow:y`) + literal は `{"value": ...}` typed object、open set = DR-094 ns)。wire 糖衣 (`env:`/`config_key:`/`default:`/`default_fn:`) は lowering が既定試行順で展開 (糖衣等価性から source タグ観測は不変)、auto 系は defaults へ要素を注入する installer。**DR-031 の席順序固定は部分 supersede** (糖衣の既定展開順として生存、canonical は宣言順試行。CLI/link 最上位は不変)。**変換 field は アンカー全明示** (CR-Q1=a): `pre_type_filters`/`type`/`post_type_filters`/`final_filters` (非 accum 専用・綴り不変、終端座の様態語)/`post_accum_filters` (accum 専用・collector 前) — DR-079 棄却案の再採用 (棄却理由「冗長で重い」はカプセル入れ子化で無効)、raw_filters 予約は配線時に本体系へ。収集 3 (`separator` 第一級化 = DESIGN §6.3「bare separator は存在しない」の supersede / `accumulator` (flatten 従属) / `collector` = registryIdentifier | object 呼び出し形 (DR-044 §2)、collector 単独宣言時の accumulator 既定は append)。**外部界面は 2 面の契約** — 値の界面 (結果型 U + cardinality、Acc は array<T> 既定) と背骨の界面 (消費トークン数 + 受理ガード面 (DR-079 §1) + 宣言済み fallback の有無)。help/completion は DR-113/104/117 の 2 経路 + パース判断に使わない読みに限る。**cardinality 導出は一方向** (repeat ∨ separator/accumulator/collector、post_accum_filters/final_filters は被判定側)、wrong-seat invalid-range 継承、**判定入力は合成後平面** (AP-Q5=b — count preset が反例)。owns は**カプセル内パスを含む集合へ一般化** (DR-042 ④/DR-061 §1。link パス DSL とは別表記系、カプセル内未知 field は DR-067 unknown-vocab)。非 accum の収集座は不成立 ⊘、type:"none" はカプセル ⊘ (issue type-none-as-capsule-absence 参照)。2 層構造 (セル 1 組 + 枝別は type プリセット)、AP-Q4=a。合成 field 単位 1 段 (defaults/chain の {prepend,append} は明示演算子、CR-Q3=a)。**multiple 束ね名 + multiple registry 廃止** (AP-Q3=b)。カプセル並びは全順序でなく DAG の正規線形化 (宣言の読み方) で、実行時経路は値源 × 型の動的分岐 (§6、確認規定)。**collector 後の共通 filter 座 (mid=57 案) は不採用** (U の型が collector ごとに変わり 1 属性 1 registry が崩れる = 旧 cell_filters の再来。将来需要はカプセル外の値述語席で) / 時点軸名 / 作用対象維持 / 枝別ラダー / multiple registry 温存も不採用
- [DR-140](DR-140-value-capsule-wire-projection.md): 値カプセルの wire 射影 (AP-Q2=a / AP-Q6=a、kawaz 2026-08-16) — カプセルの wire 属性名は **`value`**、フル入れ子 + **type の string 縮退形** (`"value": "number"`。縮退はカプセル名の下なので要素直下二形の再生産 (opus M2 警告) に当たらない)。**構造型式のインライン書きは不可** (definitions.types / registry で命名して type 参照 — カプセル field の key 空間と value_type タグの衝突回避)。**const 吸収**: 旧 `value:` 属性は `value.const` へ (`{"name":"timeout","value":{"const":30}}`、意味論不変)。**移送の一意性は時点前提** — 旧 string const (`"value": "cli"`) は新 wire で縮退形として合法パースされ型で区別できないため、一意性は「全定義が旧綴りの時点で一括移送」(v1 前一斉更新) が担保し、紛れ込みは台帳の検知 lint / pin で塞ぐ。**要素直下の廃止**: type / 座席 4 (新綴り pre_type/post_type/final/post_accum) / multiple (解体) / value。供給 4 綴り (env/config_key/default/default_fn) は**カプセル内の糖衣キー**として存続し canonical は defaults 配列 (DR-139 §1.1)。旧直下綴りは DR-067 語彙層で unknown-vocab、互換 alias なし。**判別式**: グループ宣言 entry (DR-113 §8.1) は「value を持たず」へ、DR-135/DR-130 §1b の「値を持つか」は **presence 単独で取れず「value 不在 または value.type == "none"」** (opus C4 — none は wire 上 `"value": "none"` を持つ。none をカプセル不在へ畳む案は issue type-none-as-capsule-absence)。**lowered 断面はフラット維持で語彙だけ追随** (DR-063 §3)。schema/REFERENCE/fixture 全数移送は移送台帳 — multiple 解体分の expect 不変は移送時実測。縮退なし一律入れ子 / トップレベル type 糖衣温存 / フラット wire / 構造型式インラインを不採用
- [DR-057](DR-057-alias.md): alias — 独立要素の別入口 (参照ファミリー 3 人目: ref/link/alias)、name 導出入口は再導出継承・明示綴りは非継承、結果キーは canonical のみ

## 制約

- [DR-012](DR-012-constraints-as-attributes.md): 制約は要素属性で表現 — 評価意味論は DR-047、語彙拡充は DR-055
- [DR-055](DR-055-constraint-vocabulary.md): 制約語彙の拡充 — conflicts_with (名指しペア排他、対称)、値依存は値の枝への requires 合成 (新語彙ゼロ)、requires 語彙維持、constraint installer
- [DR-047](DR-047-constraint-evaluation-layering.md): 制約評価のレイヤリング — 遅延述語は完全経路の成立条件 (経路フィルタ)、required は値充足 (default 込み)、exclusive_group / requires トリガは committed — 値空間なし要素の required / requires 目的語判定は refined by DR-093 (型委譲)
- [DR-093](DR-093-required-type-directed-satisfaction.md): required / requires の充足は型委譲 — 値空間ありは値の有無 (DR-047 §5 不変)、値空間なし (`type: "none"`、dd 含む) は発火 (committed)。requires 目的語も同枠 (DR-089 §4 の definition-error を置換)、dd → greedy Seq 宣言語彙化 (DD2) は PoC 実測のうえ不採用のまま idea 降格
- [DR-103](DR-103-required-group.md): 必須選択グループ — `required_group` (要素側属性、`exclusive_group` と同型の `Array[String]`、DR-047 §5 の値述語に分類、判定は `required` 単項充足 (DR-093) のグループ論理和)。名前空間は `exclusive_group` と独立、同名併用で exactly-one (tar のモード必須) を合成可能。単独 member は `required: true` と等価に縮退。definition/scope 側の `groups` 1 級座席は棄却 (kawaz 裁定「グループ判定は評価側が group ラベル集約で足りる」)。DR-012 の「グループ的必須は or+required で足りる」/ DR-055 §4 の同主張は独立トリガ群 (flag 群) には不成立と判明 (journal 実証) し Superseded 節追記を提案

## 継承

- [DR-013](DR-013-inherit-inheritable.md): inherit / inheritable で階層継承 — **superseded by DR-124 / DR-125** (inheritable 側は DR-124、inherit 側は DR-125 が廃止し、2 フィールドとも対象を失う)
- [DR-059](DR-059-inheritable-prefix.md): inheritable の prefix 生成 — 定義スコープ名 1 個の固定 prefix (全祖先同綴り)、衝突は実行時 ambiguous、別綴りは alias、lowering は global の逆方向コピー — **superseded by DR-124** (inheritable 廃止で全条項が対象を失う)
- [DR-124](DR-124-remove-inheritable.md): inheritable の廃止 — 祖先スコープからの書き込みは「外側スコープの通常 option + 内側要素の `inherit: {"from": ...}`」で書く (kawaz 裁定 2026-07-29)。1 段の実需は既存語彙の合成で閉じ、固有価値は多段チェーンへの入口自動配布のみ。子の宣言が祖先の表面 (help / 補完 / 結果キー) を変える遠隔作用と、未規定のまま積んでいた交差面 (per-copy export_key opt-out / alias × inheritable / world 境界) を仕様から落とす。inherit 席 (DR-031 第 4 席) は不変、global (親→子孫) は片翼で存続 (対称性それ自体は利用者の要求ではない)。DESIGN §11.3 は欠番として空け §11.4 の番号は動かさない — **§2 の代替パターンの綴りと §3 (inherit 存続) は DR-125 が更新** (祖先参照は `default_fn: "borrow:..."`)
- [DR-125](DR-125-remove-inherit.md): inherit の廃止 — 値源ラダーは 4 段 (CLI/link > env > config > default) になり、祖先スコープの値を既定値にする定義は「外側に `default` 付きの通常 option + 内側の `default_fn: "borrow:<外側要素名>"`」で書く (kawaz 裁定 2026-07-29「同じことを別機能として残す意味ない」)。wire 属性 `inherit` (bool 形 / `{"from":...}` object 形)・inherit installer・`sources` タグ語彙の `inherit`・`cell_fns.inherit` を落とす。`cell_fns` の `inherit` は descriptor の全軸が `borrow` と一致する重複住人で、参照方向を含意しない `borrow` (DR-114 §8 の canonical 住人) を残す。`borrow:<source>` の名前解決は ref / link と同じ lexical scope chain (DESIGN §2.7) — 新規則ではなく既存規則の適用範囲の明文化。同名祖先の暗黙探索 (`inherit: true`) と「内側にも固定 default を持ちつつ外側から一括指定」は代替を持たない意図した縮小 (`default` と `default_fn` は同じ席で併用不可のため、既定値の置き場所は外側 1 箇所に決まる)。DESIGN §11.2 は §11.3 と並ぶ欠番、LOWERING §B.7 は削除
- [DR-014](DR-014-config-field.md): config フィールドで階層継承可能な設定
- [DR-031](DR-031-value-source-precedence.md): 値源の優先順位 (CLI/link > env > config > inherit > default、固定) — required の判定入力は updated by DR-047、source 確定ルール (境界条件) を拡張確定 — **inherit 席は DR-125 が削除** (現行ラダーは 4 段、source 語彙は 7 語彙)
- [DR-081](DR-081-default-seat-rewrite-and-source.md): default 席書き換えモデル — env/config/inherit は default と default_source (観測用隠し属性) を書き換える、source = committed ? cli : default_source、op=default は「書き換え済み default」を明示 set で source=cli (DR-031 明文の再確認、fixture 実践の由来席読みを覆す)
- [DR-083](DR-083-multiple-declared-default.md): multiple 要素の宣言 default — 尊重される (反復系の [] は宣言不在時の暗黙 default)、供給形は分割済み pieces (DR-050 config array 同型、separator 非適用、merge マーカー不活性)、op=default は書き換え済み default へ (DR-081 既定)、scalar への配列 default は definition-error (invalid-range)
- [DR-084](DR-084-multiple-ref-row-accumulation.md): multiple×ref — 累積単位は発火値 = row (append で [row...]、0 発火 [])、repeat×multiple は発火境界保存の T[][] (flatten 不採用、平坦形は multiple 単独で表現)、merge×ref は definition-error (invalid-range、マーカーの認識対象 piece 列が構造上不在)
- [DR-085](DR-085-regex-match-host-dialect.md): regex_match — Validate filter (pattern 1 引数、unanchored=部分一致)、照合方言は host 実装準拠 (DR-040 の方言一致記述を相対化、fixture は方言安全パターン限定)、compile 失敗は definition-error (kind=invalid-argument 新設)、colon 含み pattern は DESIGN §8.4 既存オブジェクト形式 (短縮形の colon 全分割は不変、name 特例 / descriptor 分割制御は不採用)
- [DR-086](DR-086-value-slot-consumption-priority.md): variant 枝競合の消費優先 — 値スロットが照合消費できる読みは引数なし枝に cut で優先 (後続 positional の充足可能性で再解釈しない)、required positional 同居は missing_operand / optional 同居は [] で success (ambiguous 化しない)
- [DR-087](DR-087-lazy-default-resolution.md): default の遅延解決 — default = 全解決後に空の cell へ入る fallback (先詰めモデルの否定)、op=default / 席書き換えは placeholder 操作で実体化は依存順に最後 (config_path 解決 → config 席参照 → 祖先 → inherit)、解決後の消費者は常に全実体化済みの値を見る (再演不要・不可)
- [DR-088](DR-088-declared-source-is-default-presence.md): 宣言された値源はデフォルトの存在 — env/config/inherit 宣言 = 遅延 default_fn と同型、経路探索の値充足述語は静的宣言ベース (committed ∨ 宣言あり、実行時値に非依存で決定的)、最終判定は遅延解決後の実値 (空なら unset のまま落ちる、再探索なし)
- [DR-089](DR-089-type-none-value-space.md): type 省略 = none — type は値空間のみを規定し消費は構造の関心 (直交)、none = 値空間が空の node (消費 0 の純トリガも「食って捨てる」も構造で書ける)、結果非掲載で発火観測は ParserContext / explains 層、値充足席には立てない (definition-error) — required / requires 目的語については superseded by DR-093 (発火=committed で充足)
- [DR-090](DR-090-dd-pattern-trigger-self-keep.md): dd の一般化 — トリガ形 (exact | match 正規表現、host 方言準拠) × 自己の扱い (drop | keep = 消費 0 で自身含め positional 域へ) の 2 軸。優先規則は新設せず pattern 設計 (xargs 型は `^[^-]` = 最初の非ハイフン operand) で競合自体を回避、option 面の終端は severed の効果が与える
- [DR-091](DR-091-bare-key-value-operand-stages.md): bare key=value operand の段階表現 — §1 素通し (multiple string + regex_match、既存語彙)、§2 kv_map accumulator (Map へ畳む・last-wins)、§3 固定キー型付きは long_prefix:"" + require_equal_separator 新設【DR-096 で キーは long_eq_sep へ置換、§3 の空 prefix 合法条件は撤廃 (先食いが衝突を解決)】
- [DR-096](DR-096-scope-config-axis-reorganization.md): option 表面 config の軸別再編 — `long_eq_sep` (3 値 `require`/`allow`/`deny`) / `short_attached_value` (4 値 +`last_only`) の enum 化。旧 `allow_equal_separator`+`require_equal_separator` の 2 bool を `long_eq_sep` に統合 (illegal states unrepresentable、矛盾 definition-error 規定と fixture を削除)、`short_attached_value` 新設で gcc/clang 型 per-option attach-only と GNU getopt 型 last_only (付着は丸取りのみ) を表現、`long_prefix:""` の合法条件 (DR-091 §3) を撤廃 (先食いが operand 衝突を解決)、ダイヤルに位置条件なし、`short_combine` は不変
- [DR-100](DR-100-config-key-prefix-normalization.md): config 語彙の整理 — `auto_env` を `env_auto` へ改名 (DR-096 の軸 prefix 先頭規律を env 軸にも適用)、factory config キーの canonical は「factory 名 prefix あり」と規約化し `builtin/bool_parser` の `true_values`/`false_values`/`case_insensitive` を `bool_true_values`/`bool_false_values`/`bool_case_insensitive` へ追随リネーム (`number_parser`/`int_parser` は既に準拠で不変)。bare 統一案は不採用 (綴りの好みは vocab_alias installer 構想がユーザランドで吸収、正準は動かさない)、dd の `match`/`self` への `dd_` prefix 付与も見送り
- [DR-123](DR-123-repeat-cell-initialization-and-bottom-default.md): 反復セルの初期化位相と暗黙 bottom default — 反復セル (repeat / multiple / `optional:` 糖衣) の `[]` は初期値ではなく**初回発火の産物**で、発火するまでセルは空席 (kawaz 裁定 2026-07-28「初めから [] があると見るのは間違い、初めてトリガされた瞬間に [] 初期化されると見るべき」)。未発火セルは通常どおり値源ラダーが回り (DR-083 §4 を repeat 含む反復セル全域へ一般化)、`default:` は影にならず到達可能な席になる。「0 発火なら `[]`」は独立した結果整形規則でなく**ラダー最下段の暗黙 default** への配置換えであり、DR-051 §2b (反復系は absent にならない) は独立規則でなく §2a (default 持ちは absent にならない) の系になる。`repeat:{min:1}` + `default:` は 0 発火のまま完全経路になり default がセル全体を埋める (`min` = 消費読みが成立するときの内部制約 (DR-043)、`default:` = その座を消費で埋めなくてよいという充足判定 (DR-088 §1) で層が違う)。`unset` のラダー開放 (DR-045 §2) が下位席無しで `[]` に落ちる既存 pin が本モデルから再導出される。`[]` を初期値と見て `default:` を到達不能な影とする案 (REPDEF-Q1 起票時の推し) と、`min:1` + `default:` を definition-error にする案を不採用

## CLI 入口 / variant / filter

- [DR-009](DR-009-filter-chain.md): filter chain 初期形 — reorganized by DR-034 (pieceProcessor + separator + accumulator + collector に再編成)、@base sentinel は superseded by DR-062
- [DR-062](DR-062-filter-inheritance-interface.md): filters の継承インターフェース — @base sentinel 廃止 (発見不能な in-band 特殊値・孤立語彙)、string 短縮形 (差し替え) | object 詳細形 ({prepend, append})、ref 継承は後勝ち上書き、中間挿入は非対応 (type shadow で差し替え)、命名 (pre_filters) は superseded by DR-079
- [DR-079](DR-079-filter-seat-lattice-and-artifact-anchored-names.md): filter 座席の完全格子と作用対象アンカー命名 — pre_filters/filters/post_filters → piece_filters/value_filters/cell_filters (pre/post 全廃、raw→piece→value→cell の artifact 進行)、raw_filters (分割前座席) は名前予約のみ、registry 名 filters は語彙プール専用に純化、互換 alias なし
- [DR-011](DR-011-variant-dsl.md): variant の文字列 DSL とオブジェクト形式 — set の args なし形 (主入口) は updated by DR-071
- [DR-071](DR-071-long-variant-list.md): long の責務分離 — variant リスト一級化 (各要素が入口 1 個、`:set` = 主入口) + `long: true` は [":set"] の糖衣、absent = false = [] = 入口なし (presence 罠の構造的消滅)、主入口なし variant のみが表現可能に、旧 `long: []` の意味変更 (破壊的、ドラフト期)
- [DR-045](DR-045-effect-descriptors.md): 効果記述子 — 値セル操作は純データ (set/default/unset/empty、committed は効果が明示制御)、効果列の判定キー精密化 — **op 語彙と committed の制御は updated by DR-131** (unset は `set(null)` へ畳み、empty は `Sentinel(Empty)` と観測 op を維持。set の committed は operand が null かで決まる)
- [DR-131](DR-131-sentinel-reduction.md): Sentinel を `default` / `empty` の 2 住人へ縮小 (2026-08-02 EMP-Q1=a 追補、**前提は DR-130**) — **`unset` は `null` を返す Value fn** (`set(null) = unset`、Sentinel としての unset は消滅、wire の綴りは不変)。set の operand が null なら committed=false でラダーを開放し、それ以外は true。**`empty` は `Sentinel(Empty)` を返す cell fn** で、array / map / record を committed=true で空にし、scalar・union・その他は definition-error `invalid-range`。Value fn 化しても観測 op `empty` の温存・対象型依存の空値決定・`abi.Value` 複合化依存という 3 特別視が残るため差し戻した。effects 語彙は 5 op (`set` / `default` / `remove` / `splice` / `empty`) で、unset 発火は `{"op":"set","operand":null}`、empty 発火は `{"op":"empty"}`。default 席は Value 返し fn だけを受け入れるため `default` / `empty` は definition-error `invalid-range`、空配列の既定値は `default: []` で表現する。DR-127 §4.1 の値残余座でも Sentinel 返しの `default` / `empty` は Reject、`unset` は null Value として適用できる。値の不在を返す borrow、行供給座の null、null 素通しと cell 適用時の unset 化は DR-130/本 DR §1〜§2b の一般規則に従う。
- [DR-101](DR-101-unknown-filter-definition-error.md): filter 名の未登録は definition-error (kind=unknown-vocab) — value_filters / piece_filters / cell_filters (非 accum 位置) の 3 属性で filter 名が filters registry の descriptor `owns` 集合に載らない場合は `parse_definition` が静的 reject (runtime reason `unknown_filter` は wire から消滅、DR-042 の filter/installer 同格 + DR-054 §1 の「lowering が構成できない = Error」の filter 側適用)、専用 kind は新設せず DR-054 §4 の既存 8 kind の `unknown-vocab` に吸収 (DR-082 §2 の受け皿規定を直接適用、long update transform 名検査 DR-077 §2 と同族)、filter 装置内の invalid-argument (DR-085 の regex_match compile 失敗) とは別層 — **§3 (accum 位置の cell_filters 判定マトリクス) は DR-102 で superseded** (cell_filters 属性分割により位置依存判定の前提が解消)
- [DR-102](DR-102-filter-attribute-split.md): `cell_filters` の属性分割 — multiple 有無で T→T/Acc→Acc という異なる語彙を 1 属性に内包していた構造的欠陥を解消し、`final_filters` (非 multiple 専用、T→T、最終値ガード) と `accum_filters` (multiple 専用、Acc→Acc、累積配列) に分割 (kawaz 裁定 2026-07-13、SPL-Q1〜Q6 裁定バッチ)。1 属性 1 registry で unknown-vocab 判定が単純化、位置依存の層違い invalid-range 判定は消滅。multiple 有無との排他はいずれも definition-error kind=invalid-range (scalar-array-default-invalid-range.json と同型)。reject の argv_pos は両属性とも argv.length 帰属 (value_filters の piece 実位置帰属との違いが独立属性として残す実証根拠)。非 multiple 要素の宣言 default 値の pieceProcessor 通過は DR-050 §4 の型依存規則をそのまま適用 (config 供給と同型)。DR-079 (座席格子 D 行分割) と DR-101 §3 に Superseded 節追記。旧 DR-102 (非 accum×ARRAY-only=invalid-range の判定精密化案、未 push) は前提から棄却され abandon、番号を本 DR が再利用

## help / 補完 / 表示メタデータ

- [DR-058](DR-058-hidden-deprecated.md): hidden / deprecated の挙動 — hidden は help/補完から除外 (受理不変)、deprecated は受理 + ParserContext.warnings (v1 bool のみ、表示はレンダラ)
- [DR-060](DR-060-completion-query.md): 補完クエリ — 生存 partial 経路 (dead end 除外) の期待集合の和集合、after 整合フィルタ、素材+メタ返却でポリシーは生成器、completer は名前参照で shell 機能へ委譲、責務 4 層
- [DR-112](DR-112-help-query-and-model.md): Superseded by DR-113 — help installer 不在・独立 help query を中心とした旧設計
- [DR-113](DR-113-help-mechanism-redesign.md): help 機構の再設計 — help_installer の回収/植え付け/能力提供、5 直交 type、value_structure tree + type_ref + types + origin、DR-114 の cell_fns を使う default_fn、group/order/on_failure/conformance の確定 (§1 回収語彙・§8.1 グループ宣言 entry hidden 許可は DR-115 が update)
- [DR-115](DR-115-canonical-help-renderer.md): canonical help レンダラ — `help_render` 席の 3 段 override (wire 一括 / entry 個別 / API style)、セクション骨格はプレースホルダ文字列テンプレ (置換 + `{{` エスケープのみ・制御構造なし・識別子は閉集合 8 種、移行のバイト互換需要が決め手)、文言内 binding 補間は `{name}` のみ (テンプレ位置 strict / 文言位置寛容の非対称)、グループ宣言 entry の hidden 許可 (DR-113 §8.1 update、category_mode default = 入口注記 / all = 表示、show_hidden と直交)、canonical 既定 = value_structure/types とも auto・origin は separate_section (gh INHERITED 型)、conformance は受理・搬送・model `render` 射影まででテンプレ適用後バイト列は非規範 (render profile 不設置、バイト再現の完全制御は言語側 closure)、completion 表示 policy は DR-116 が確定
- [DR-116](DR-116-completion-generator-policy.md): canonical 補完生成器の既定 policy — definition 由来候補は DR-113 §8 適用済み help model 順、completer 由来候補は供給順、lazy は runtime 問い合わせ ABI の関心で core 契約不変、説明は candidate に同梱せず origin から引き直す、hidden 除外 / deprecated 表示 + 注記 / alias 表示、専用 order・description・complete_lazy 語彙を設けず生成器出力は非規範で spec conformance 増分ゼロ — 増分ゼロの読みは DR-117 §8.1 が精密化 (preset 化の増分は DR-117 の裁定由来)
- [DR-117](DR-117-completion-generator-abi.md): 補完生成器 ABI — ブリッジ型一択 (v1 shell = zsh/bash/fish)、生成入口は `type: "completion_script"` preset (shell 名必須値・values enum なし + 候補提示の糖衣・on_failure 既定 false・positional 入口は全 preset で許可 = ABI-Q1 裁定の一般則)、query は `KUU_COMPLETE=<UUID>` env と argv[1] の UUID 二箇所一致 (完全一致のみ、不一致 = 通常実行と観測等価、`KUU_COMPLETE_INDEX` で cword・省略 = 行末)、words はコマンド名先頭込み全量、応答はタブ区切り行指向 (nospace 候補フラグ / `:shell_action` directive / エスケープなし = 搬送不能候補除外)、policy 適用は binary 内 1 箇所・prefix 絞りゼロは空リスト、形態 B (kuu-cli) は正規サブコマンドで capability 露出 + completer 縮退は候補なし + validate 報告、builtin completer `files`/`dirs` 収載 (io_type は先送り継続)、conformance 増分は preset 受理面のみで新 profile なし

## conformance / 直列形

- [DR-063](DR-063-atomicast-serialization.md): AtomicAST 直列形の確定 — wire form = 宣言層のみ (A 群適用済み + installer 語彙 inert、lowered は決定的再導出)、lowered 断面表記 (面構造 / matcher kind + name 参照 entries / 効果記述子 / `#` 予約内部 id) は段階別 fixture 用で緩比較、構造等価で byte 一致不要
- [DR-065](DR-065-conformance-fixture-format.md): conformance fixture フォーマット — why 必須 / query タグ (parse 定義、lower・complete・definition_error 予約) / definition は wire form / expect は DR-053 union 転用 (effects = cli 効果のみが正本、result = ラダー込み最終値) / error kind 割当 (構造的不足・残余 = parse) / 機能領域別ディレクトリ。正本は docs/CONFORMANCE.md
- [DR-082](DR-082-definition-error-fixture-format.md): definition_error fixture format — DR-054 §4 返値の転用 (element+kind の集合比較、message/hint 非比較、argv なし)、未対応構成系の kind は invalid-range (DR-065 予約の解消)
- [DR-066](DR-066-error-reason-codes.md): 実行時エラーの reason コード層 — errors に機械可読な reason 追加 (DR-053 拡張、組み込み発生源は必ず emit)、発生源は descriptor の reasons で宣言 (DR-061 拡張、完備チェック / typo 検出 / L10n の基盤)、組み込み最小語彙 (missing_operand / unexpected_token / constraint 4 種)、fixture は optional 検証
- [DR-067](DR-067-wire-well-formedness.md): wire form の well-formedness — 合法性 3 層 (構文 / 語彙 / 参照)、空 or・空 seq・1 児・トリガ重複は合法 (warn は lint)、name は非空のみ制約 (`#` id はユーザ禁止の予約)、multiple の配置制約なし (F-042 解消)
- [DR-068](DR-068-json-schema-lifecycle.md): JSON Schema と spec バージョンの lifecycle — 確定版 v1.0.0 = 参照実装 fixture green (フェーズ 3)、バージョン単位は spec バンドル一体、semver (語彙追加 = minor だが旧実装は unknown-vocab で正しく拒否、意味論変更 = major)、$schema は確定版から (F-048 解消) — 準拠宣言の単位は updated by DR-069
- [DR-069](DR-069-conformance-profiles.md): 準拠プロファイル — 段階準拠 (parse-core 必須 / lowering / definition-error / completion opt-in)、descriptor の実装要求は owns + reasons のみ (observes / factory 一般化は canonical 実装の装備 = 準拠非要求)
- [DR-070](DR-070-lower-fixture-format.md): lower fixture フォーマット — installers 列挙 (省略 = 全登録、順序非規範)、expect = DR-063 §3 断面の緩比較 (内部 id 綴りと席順は無視)、順列検査は runner 組み込み 2 段 (常時 = 決定的少数 / opt-in = 全順列)、粒度は基本 3 点セット + 厳選組合せ
- [DR-072](DR-072-fixture-case-id.md): fixture case の安定 id (slug) — 各 case に required "id" (kebab-case・fixture 内 unique・意図 2〜4 語、通し番号禁止)、参照は rel::slug (位置依存 case#N の置換)、id 一意性は fixture メタ層の lint (DR-067 の wire 3 層とは disjoint)、id 欠落は fixture 不備
- [DR-104](DR-104-completion-fixture-format.md): complete fixture フォーマット (DR-065 予約の解消、v1 blocker 化) — 入力 args_before/args_after (word_before/word_after は v1 未使用予約、DR-060 §2 の before/word/word_suffix/after から改名)、candidates は Cand 構造の直訳 (spelling/is_value/ty/origin/term/meta 必須・completer は opt-in・path は wire 非搭載) の集合比較、候補同一性は spelling/is_value/ty/origin/term/meta の完全一致 (path は同一性に不参加、実装既定を格上げ)、遅延述語は args_before-only の候補生存判定に不参加 (dead end=parse相/制約=resolve相)、args_after 供給時のみ完全経路判定 (遅延述語込み) で間接的に絞る
- [DR-132](DR-132-fixture-namespace-conformance-residents.md): conformance 複合値ビークル — `fixture/*` namespace の仮想型 2 住人 (kawaz 裁定 2026-08-02、DR-128 §12 の `fixture/*` residents 系統の具体化、RECB-Q1〜Q6 確定) — **`fixture/int_range`** (record `{start: int, end: int}` 産出、string 形は最初の `..` で 1 回だけ分割する全体一致 `A..B` / `A..` / `..B` のみ受理 (バックトラックなしの決定的分割)、`..` 単体・単独 `A`・構成部 int 不適合は reason `not_an_int_range`。部分形は欠落フィールドをキーごと持たない部分 presence を産出 = DR-127 §3.1 の実体、null 補形は射影層 = DR-130 §4.1) と **`fixture/json`** (string → value、RFC 8259 で読めない綴りは `not_json`、`value` 宣言座の実行時解決系 = DR-127 §2.2 の map/value 行を代表 pin)。いずれも実用型でない **conformance 専用の仮想型** — 提供義務は conformance 実行文脈での解決可能性のみ、通常 registry への常設は実装裁量かつ安定性保証外 (実用の timespec 系複合値型は将来の別 DR)。descriptor は `schema/builtin-descriptors.json` の types 区分に fixture ns 住人として収載 (bare 糖衣なし = DR-094、construction: static でダイヤルなし、構成部の受理輪郭は canonical default の int)。パースの担い手は経路で変わる — string 形は本住人が構成部失敗も `not_an_int_range` に畳む (number_parser の 1 綴り帰着慣行と同型)、link 注入経路の operand はフィールド型 `int` のパーサが読み `builtin/int_parser` の reasons が発生源どおり emit される (DR-127 §3.2 の判別可能性がそのまま pin 素材)。複合値の conformance 綴り 3 点を確認的に固定 (result = 補形適用後 / sources = 座単位の構造分解で link 部分書き座だけ `link` タグ / **effects operand = 補形前の生出力**で missing ≠ null 区別が operand 内部にも及ぶ)。DR-127 波及 fixture (2)(5)(6) を int_range、(4) を json が担い (record は vivify するので (4) は書けない)、fixture 追加自体は kuu.mbt 実装と同じ lockstep 窓。3 住人案 (timestamp/timerange) / 汎用 configurable factory / `builtin/struct` 前倒し / `..` 単体の空 record 受理 / 単独 `A` の暗黙読みを不採用
- [DR-108](DR-108-spec-release-process.md): spec リリースプロセス — VERSION ファイルが spec バンドル一体 (DESIGN+LOWERING+CONFORMANCE+REFERENCE+DR+fixtures+schema) を代表、kawaz 標準 release ループ縮小形 (check-version → validate-bundle → release の 3 job、bump → push → CI が tag/GH Release、v1.0.0 未満は prerelease)、権限最小化 (release job のみ contents:write、persist-credentials:false、bump-semver はバージョン固定取得)、concurrency 直列化 + gh release create 直前の同 tag recheck、bootstrap 判定の fail-closed 化 (stderr 内容で空/エラーを区別)、V1-Q1=b の正式記録 (DR-068 §1「fixture 全 pass」= DR-069 4 プロファイル全 green を**指定参照実装 kuu.mbt**で満たすこと、DR-069 §1「parse-core 最小」は実装の準拠名乗り条件で別軸)、green の規範・証跡ファイル (`docs/releases/vX.Y.Z-evidence.md`) は CONFORMANCE §0.1 + validate-bundle の軽量 gate、schema `$id` は wire/fixture/descriptor の 3 ファイルのみ (builtin-descriptors.json は `$schema-ref` 更新)、参照実装は CI pin (SHA) + README 準拠宣言で対応、v1.0.0 発行自体は docs/runbooks/v1-release.md 側で別途実行 (codex レビュー #5 の C-1/M-1〜M-7/m-1〜m-3 反映)

## レジストリ / 実装連携

- [DR-010](DR-010-external-registry.md): 外部レジストリの階層化と暗黙参照 — updated by DR-035 (definitions/registry 一様化), DR-036 (multiple registry 追加), DR-040 (type 方言の3層上書き)
- [DR-016](DR-016-result-and-context.md): 結果オブジェクトと ParserContext の2層 — required の判定入力は updated by DR-047
- [DR-035](DR-035-definitions-registry-symmetry.md): definitions は registry と同じ区分の名前空間、解決順の一様化 (DR-007 を再編成)
- [DR-036](DR-036-multiple-registry-and-accumulators.md): multiple registry 追加、accumulators の属性セット拡張、collectors は filters で代替 (DR-008/010 を更新)
- [DR-042](DR-042-installer-architecture.md): installer アーキテクチャ — 特殊語彙 (long/short/env/dd) は registry 装置の所有語彙、5 不変則で順序非依存合成、値源はラダー席宣言
- [DR-049](DR-049-env-lookup-contract.md): env lookup の契約 — env_provider は単一スロット `(key) → string | null` (null=未設定、prefix 連結済み key)、env 値は pieceProcessor 通過、auto_env (DR-100 で `env_auto` へ改名) はフル修飾導出で明示 env: 優先
- [DR-050](DR-050-config-file-value-source.md): config ファイル値源 — type: config_file の配線宣言、config_provider は `(path) → 階層オブジェクト | null` (フォーマットは provider の関心)、config_key は同型対応デフォルト + link パス DSL の明示上書き、値の型は要素の type、config は構造に影響しない
- [DR-133](DR-133-config-path-list-fold.md): config パス列の fold (CFM-Q1a + CFM-Q2、kawaz 2026-08-12 / **CFM-Q3a + CFL-Q1a で再改稿 2026-08-14**) — **1 スコープ 1 config_file、並置は definition-error `invalid-range`** (関与要素ごとに 1 件全列挙)。config 席はスコープに 1 つ (DR-031 #3) なので、2 つ以上の config_file は同じ 1 席への重複宣言 = 席の値域外。kind は DR-134 §2 と同じ「同居不可の組合せ」の筋で、export-key-collision のような hint 出し分けを持たないので新 kind は興さない。**重ねの正規形は 1 要素の `multiple`** (`{type:"config_file", multiple:"append", default:["/etc/app.toml","~/.app.toml"]}`) — 並置の 動機は全部 1 要素に収まる (綴り違いの入口は alias、由来違いの入口は元からラダーの 3 席、既定パス列は配列 default、分割指定は borrow)。初版の要素間規則 (全 config_file 要素を宣言順に走査して 1 本の列にし multiple は inline 展開) は **mid=8 の誤読として撤去** — 並置に与える意味がどれも 1 要素 multiple の再発明になり、同じ重ねが 2 通りの綴りで書けて要素間の宣言順という観測軸を増やしていた。**席埋め規則は「パス列 → 1 オブジェクト」の 1 本**: その要素が供給するパスを供給順に並べ (単値は列長 1 の縮退形)、列の各パスを順に provider へ渡し (契約不変 = 1 パス 1 オブジェクト)、読めたオブジェクトを**トップレベルキー置換**で畳む。後のトップキーが前を置き換え、後に無いトップキーは前の値が残る。**深いマージはしない**、v1 にダイヤル語彙も 持たない。**committed 読込失敗 Error / default 由来不在の黙認は各パス独立**に判定し、前のパスの committed 性が 後段に影響しない。**committed 判定は link を透過する** (CFL-Q1a) — 経路でなく値の源席の cli/env 性で判定し、link は明示供給の搬送路にすぎない (DR-031 の同順位規定)。ただし **sources の観測タグは `link` のまま** (DR-121 §4 は現役) で、由来を辿るのは committed 判定 (内部) だけの別軸。**排他選択と重ねは `multiple` の 有無で書き分ける** (どちらも要素は 1 つ)。パスを供給しなければ列は空で config 席も空 (エラーではない)。**結果面では config_file 要素自身も通常要素**として確定パスが result / sources に座り、`multiple` なら **供給順のパス列 string[]** — 読めたパスだけを残した列ではない (DR-135、値を決めるのは要素のラダーで provider の返り値ではない)。並置放置 (CFM-Q3b) / 全体置換 (CFM-Q2 a) / 先勝ち (CFM-Q1b) / 深いマージ (CFM-Q1d) は不採用。異なるスコープに宣言された config_file の関係と、候補パスの探索 (「最初に読めた 1 個」= file 要素 + readable filter + borrow の合成) は射程外
- [DR-135](DR-135-config-file-is-a-normal-element.md): config_file は通常要素 — 内部セルの特別枠から外す (kawaz 2026-08-14) — DR-121 §2.1 が `type: "none"` / `config_file` / dd trigger を一括で内部セル (`effects` / `result` / `sources` のいずれにも現れない) としていたが、**分界は値を持つか**であり 3 者は揃っていない。none は値空間を持たない型、dd は値セルも子も持たない trigger で「値の無い構造マーカー」だが、**config_file は string のパス値を自分の cli > env > default ラダーで確定させる普通のセル** (DR-050 §1 「パス要素は普通の要素」) なので特別枠を廃する。**name があれば result / sources に出る** (値 = 確定したパス、`multiple` なら供給順のパス列 `string[]`、0 供給は `[]`、未供給の座は `null`)。sources は自分のラダー席のタグで、**config 席は循環禁止 (DR-050 §5) のため `config` タグは立たない** (不変)。**result に座るのは「供給されたパス」であって「読めたパス」ではない** — DR-050 §5 の依存順序が「値確定 → provider 読込」と段を分けており、読めたかは fold (DR-133) の関心で列自体を書き換えない。**`effects` にも通常要素として載る** (cli / link 由来のパース時効果のみ、env / default 由来は従来どおり非掲載。effects は id 軸 (TRG-Q3=a) なので透過 config_file の cli 発火も載る)。**露出キー衝突検査に占有子として参加** (DR-120 §4 の占有側。§1.1 = DR-120 §4 / DR-130 §1b の非占有列挙に config_file は元から無く、特別枠は DR-121 §2.1 と CONFORMANCE §2 の 2 箇所だけの分裂だった → 占有の正本側へ揃える)。値持ち command の内側に置く形は DR-134 §2 の占有子行に落ちて `invalid-range`。隠したい定義は `export_key: null` (透過、値の伝搬は止まらないので config 席の充填は働く) か nameless で通常語彙により制御する — したがって config_file の `export_key` は dd のような inert ではなく**実効** (DR-134 §6 の線との対比)。「出すが占有しない」は DR-120 §1 (1 キー 1 セル) が破れるため不採用、config_file 専用の出す/出さないダイヤルは `export_key` 軸の二重化 (DR-052) で不採用。id 軸の一意性 (config_file `user` + option `user` の参照識別子重複に使う kind) は別軸で、DNR-Q1=a により `duplicate-id` に確定 (DR-054 更新 5)
- [DR-056](DR-056-vocabulary-ownership-vs-reference.md): 宣言語彙への関わり方 — 所有 (lowering 責務、排他) と参照 (advisory read、自由)。参照の成果は観測挙動に影響してはならない — 機械可読化は DR-061 (descriptor)
- [DR-061](DR-061-registry-descriptor-and-configurable-factory.md): registry 装置の自己記述 — installer descriptor (owns / observes / config キー所有)、wire 追加語彙の正当性 = 所有集合の和、configurable factory ({name, config} 参照、canonical = default config)、config はキー平坦・値自由 JSON、factory config と filter の線引きは「相」
- [DR-094](DR-094-registry-vocabulary-namespace.md): registry 語彙の namespace — filter 名/factory 名/type 参照/reason 語彙全域に ns、builtin (closed set) と拡張 (open set) の分離、bare 名は builtin ns の糖衣、区切りは `/` (`:` は DSL 引数区切り・`.` は link 固定パス DSL のフィールドアクセスと衝突のため不採用)、共通 reason (too_small 等) は builtin ns 内で複数 filter が共有可能 (DR-066 §2 の緊張を解消)、definitions ローカルキーは ns 対象外
- [DR-095](DR-095-builtin-descriptor-reasons.md): builtin filter/factory の reasons 宣言集合の確定 — 正本は spec 側 `schema/builtin-descriptors.json` (SCH-Q2-a)、組み込み filter は descriptor 単位で列挙 (SCH-Q3-b、trim/increment/unique は Transform で reasons:[]、non_empty は `empty_value` 新設、in_range は `too_small`/`too_large`、regex_match は実行時 `pattern_no_match` のみ・compile 失敗は definition-error kind=invalid-argument に分離)、canonical factory は DR-094 §9 案 A の新名 (`builtin/number_parser` 等) で `not_a_number`/`not_an_integer`/`int_out_of_range`/`not_a_bool` を帰属 (DR-074/075 根拠)
- [DR-098](DR-098-tty-injected-value-source.md): tty 判定の値源化 — tty_provider は env_provider/config_provider と同列の単一スロット `(stream: "stdin"|"stdout"|"stderr") → bool | null`、wire 属性 `tty` で bool 値要素の席に注入 (供給値は native bool、型不一致・none/dd・flag/count への付与は definition-error kind=invalid-range)、値源ラダーは `default` の直前 (`inherit` の後) に挿入 (明示 > 継承 > 観測 > 宣言既定)、source タグに `tty` 追加、評価器の純粋性は不変 (ambient probe は provider 実装に閉じる)、DESIGN §13.9 の TTY 責務外記述を「tty 値の注入は射程内、能動センシング/レンダリングは引き続き責務外」に改訂 — **§3/§4/§5 は DR-099 が撤回** (Superseded 節参照)
- [DR-099](DR-099-tty-is-a-preset-type.md): tty は型である — DR-098 の wire 属性 + ラダー席モデルを preset 型へ転換 (`type: "tty"` = `builtin/tty`、bool を土台にする flag/count 同族の糖衣プリセット、DR-076 の枠)。値源ラダーは DR-031 のラダーに復元 (当時 5 段、現行は DR-125 で 4 段)、tty 型の `default` 席だけが観測優先の解決規則を持つ (source タグ `tty`/`default` は維持)。configurable factory config は `tty_stream` (必須、3 値 enum) — `tty_cygwin` ダイヤルと fold 計算・cygwin 観測は DR-129 で廃止 (観測は terminal 単一 bool へ)。preset 化により DR-098 の definition-error 3 分類・multiple×tty 未規定は構文的に不要化
- [DR-129](DR-129-tty-provider-drop-cygwin-observation.md): tty_provider の cygwin 観測削除 (kawaz 裁定 2026-07-31 + MISC-C1 確定 2026-08-01) — DR-099 §4 の `(stream) → {terminal, cygwin} | null` を **`(stream) → bool | null`** へ戻し、cygwin 判定込みの端末判定を **provider 実装の内側の責務**にする。DR-098 のシグネチャへの回帰ではなく「fold の方言を spec 側の純データ計算として保つ」という DR-099 §4 の動機自体の取り下げ。根拠は cygwin 判定が端末判定の**実装都合の穴埋め**であること (出自 = mattn/go-isatty の `IsCygwinTerminal`。Windows の素の isatty が msys/cygwin pty に false を返す穴を別関数で塞いだもので、「端末か」と「cygwin か」が利用者の知りたい別々の事実だったわけではない) と、利用者の関心が「端末か否か」の全体だけであること。**`tty_cygwin` factory config を廃止** (`builtin/tty` の config は `tty_stream` 必須のみ) — ダイヤルが切り替えるのは判定方言の選択であり、その方言が provider の内側へ閉じたあと spec 側にダイヤルを残す条件は「同一 provider の下で両挙動を spec から出し分ける」需要だが、DR-099 §3 の既定 true 根拠 kawaz/die DR-0008 は含める側を選ぶ根拠であって外す側の需要を示す材料ではない (外す側が要るホストは provider ごと差し替えれば済む)。`default` 席の解決規則は **`resolved_default = 観測 ?? 宣言 default ?? absent`** へ縮小 (`fold(観測) = terminal || (tty_cygwin && cygwin)` の計算が消滅)。観測優先の序列・source タグ (`tty`/`default`)・値源ラダー 4 段固定は不変。特殊判定が要るホストは **provider ごと差し替え**可能 (registry 単一スロット住人) なので方言を spec 語彙に持たせる必要がない。DR-126 §波及の「tty_provider を record 化する」旗艦例は出力が単一 bool になるので **record 化自体が不要**になる (DR-126 の裁定は他の消費者を持つため不変、当該箇所へ追補注記)。波及は DESIGN §3.3/§9.4 (fold 式引用)/§12b/§13.1/§16・REFERENCE §3.3 の factory config キー表・builtin-descriptors・fixture.schema (`case.tty` を `stream → bool` へ)・CONFORMANCE §1・tty-ladder fixture (cygwin dial case と `stderr_tty_no_cygwin` 削除、注入形を単一 bool へ、残 case の why の fold 記述も追随)・fixtures/value-typing/or-leaf-factory-config.json (why の fold 式・cygwin_dial 参照)・corpus/real-cli/die.json (注入形と why)。`fixtures/definition-error/tty-stream-missing.json` は影響なし (確認済み)
- [DR-106](DR-106-descriptor-role-and-carrier-axes.md): descriptor の役割軸 (`kind`) と担体軸 (`domain`) の機械可読化 — `kind` enum に `collector` 追加 (`unwrap_single`/`from_entries` を `filter` から区別)、`kind:"filter"` 限定の `domain: scalar|array` 新設 (`signature` の fallibility 軸と直交、`in_range` と `length_range` を機械可読に区別)。carrier の正本は wire 属性位置 (DR-102 §3) のまま、`domain` は descriptor 単体を読む場面の機械可読ヒント。collector の呼び出し規約 (`multiple.collector` 由来) が filter chain の colon-DSL args と異なる旨も明記。DR-036 の「collectors registry は新設しない」方針は不変 — **`kind`/`domain` は DR-107 が Superseded** (直交軸化)
- [DR-107](DR-107-descriptor-orthogonal-axes.md): descriptor の直交軸化 — `kind`→`role` rename (7 値: installer/filter/collector/type_parser/accumulator/completer/provider)、`construction: static|factory` 新設 (filter×factory = configurable filter を第一級化)、`io_type` 新設 (JSON 表現可能な値型の再帰体系: string/number/bool/null/array&lt;T&gt;/map&lt;string,T&gt;/value、固定幅なし)、`signature` を `effect: preserve|transform` + `fallibility: total|reject` に分解 (変換×失敗可能の第 3 象限を表現可能化)、`invocation: {encoding: colon_args|object_args|none, parameters}` 新設 (DR-105 §5 の definition-time 検査規則を宣言化)。role 別 oneOf で必須/禁止フィールドを Schema 強制 (collector は domain=array/effect=transform/fallibility=total を const 固定)。env_provider/config_provider/tty_provider の 3 provider descriptor を新規収載 (DESIGN §12/§12b/§14.3 の散文シグネチャを機械可読化)。DR-106 (`kind`/`domain`) を Superseded — accumulator/completer の未確定 2 行は DR-111 が確定
- [DR-111](DR-111-accumulator-completer-descriptor-axes.md): accumulator / completer role の descriptor 宣言軸確定 (DR-107 §7 の未確定 2 行の解消、全軸が既存 DR からの導出で裁定分岐なし) — accumulator は collector と同じ「構造畳み装置」の勢力図 (DR-105 §4) で const 固定 (output_mode=transform/fallibility=total/construction=static、io_type.input 常に array ラップ、invocation は object_args 固定 — `flatten` を append の parameters に載せ DR-105 §2 の wrong-seat 判定を機械可読化)、io_type の対象は畳み相 (collect) のみで cell 解決相 (resolve_cli の Binding プロトコル) は評価器内部として宣言対象外。builtin は append/merge/kv_map の 3 種収載 (increment は DR-077 の宿題決着で除外、override は概念モデル名で registry 住人ではない)。completer は named capability marker の最小形 (invocation none 固定・reasons 常時空、io_type は runtime 問い合わせ ABI 確定まで禁止 — DR-109 骨子柱 3 の $required に必要なのは name+role の同定のみ)、builtin completer は収載せず標準 completer 名の閉集合は補完生成器層 (DR-060 §5 層 2) の DR に委ねる
- [DR-126](DR-126-descriptor-record-value-type.md): descriptor value-type 体系への `record` 型追加 (kawaz チャット裁定 2026-07-31) — DR-107 §3 の「固定フィールドを持つ struct は表現できず map&lt;string,value&gt; で近似」を覆す精密化。記法は `{"record": {field: <type 参照>, ...}}` で array/map と同じ再帰体系の一員、**フィールドの型は kuu の type 参照** (registry / `definitions.types` の ns 付き識別子、wire の `type:` と同じ鍵空間) で書く — `number`/`string`/`bool` も組み込み提供の普通の registry type であって record 位置に特別語彙は無い (bool/string しか使わない定義から number を tree-shaking しても動くべき)。型は依存グラフを成し (`timerange` → `timestamp`)、未登録参照は unknown-vocab 系の definition-error。**このグラフは非循環必須** — フィールドの type 参照は DR-067 参照層が検査する type edge に含まれ、自己参照を含む循環は `circular-ref` (再帰型は v1 で不許可、JSON 形の再帰導出と codegen の停止性を単純に保つ)。JSON 形は各フィールドの type の out を再帰的に辿って導出する (registry 解決が前提、descriptor を持たない未知 ns は消費者側で `value` 縮退)。JSON 表現可能の閉域・固定幅なしの裁定は不変 (精密化するのは構造であって値域ではない)。**closed** (キー語彙の閉域、理由はリフレクション無し言語での struct 直訳保証 — open だと未宣言キーの受け皿を全生成物が持つことになり直訳が壊れる)、**フィールドは presence-optional で null 不使用** (kuu 値空間に null は無い、DR-051 §1 の absent = キー無しが record の内側へ降りる。closed = キー語彙の閉域であって全フィールド必須ではない)、**値セルの型導出の正本は `io_type.output` の record 宣言だけ** (`input_structure` の定義片は入力パーサ用で型導出に関与せず、フィールドは全て `T?`。常時 presence を機械可読に主張する手段は v1 に持たない — 実態の注記は description の領分)、**宣言と実産出の乖離は Error** (2 種: 宣言外キーの存在 / フィールドの type が名乗る out との値型違い。宣言済みキーの不在は正常)。分界文「入力→値の失敗は Reject (ユーザの世界、入力で回避可能)、返した値 vs 自己宣言の矛盾は Error (実装者の世界、入力で回避不能)」を規範化。record で書けないもの (実行時にキーが決まる object) は従来どおり map&lt;string,value&gt; / value で近似。波及は tty_provider の `{terminal, cygwin}` 近似の解消、DESIGN §12b の「struct 精密化は型体系の外」注記、DR-051 §3 の型導出が record 内側へ降りる点。link 固定パス DSL の静的化 (research 2026-07-28 §4b) が本 record を前提にするが DSL 側は別 DR
- [DR-137](DR-137-tuple-value-type.md): value-type 体系への `tuple` 型追加 (UC-Q1=a、kawaz 2026-08-16、**DR-128 採用しなかった案 (a) を supersede、DR-126 §1 を拡張**) — 記法は `{"tuple": [<type 参照>, ...]}` (2 要素以上のタグ付き形、bare array union と構文衝突しない = DR-107 §3 の予告どおりの移行)。**record と完全対称**: 座の型は kuu の type 参照 (registry 解決、未登録は unknown-vocab 系)、型依存グラフに参加し循環は `circular-ref`、JSON 形は out の再帰導出 (長さ N の array — JSON 閉域の精密化)。record が「キー語彙の閉域」であるのと対称に tuple は**位置語彙 (アリティ) の閉域**で、presence-optional は無く座は常に在り値の無い座は null (DR-130)。**全座充足が完成値の意味** (完成判定は DR-138 §1、単相の未完成確定は `incomplete_value`)。2×2 整理 (kawaz mid=32-34): キー/位置が定義時固定か × 要素型が座ごと個別かで **tuple : array = record : map**。link パス DSL では `[int]` が**定義時静的** (範囲・負 index とも — アリティが宣言で閉じている) で **vivify 可** (器 = 全座 null の array、DR-127 §根拠「closed が器の形を定義時に確定させる」の位置軸適用。array 行の vivify 不可は不変)、座への set の operand は座の type のパーサが読む。乖離検査は DR-126 §4 と同系 (長さ不一致/非 array = `output_shape_mismatch`、非 null 座の型不一致 = `field_type_mismatch`、null 座は正常 — 新 reason 語彙は興さない)。導入根拠は**出力側の位置付き部分構築** (DR-138 の枝並行構築が `[0, null, null]` の座を要求) — DR-128 棄却の入力側理由 (「pair は定義片の seq が表す」) は不変で input_structure の役割は変わらない。固定長 array + 全座必須注釈の近似 (vivify 例外・座別型欠落・DR-102 の線) / record への位置キー畳み込みを不採用
- [DR-128](DR-128-type-input-structure-splice.md): 型入力構造の splice — `type_parser` descriptor に optional 軸 `input_structure` を追加し、type が**自分の CLI トークン消費文法**を kuu の定義片 (or / seq / typed leaves、**wire の正規形 node** = A 群糖衣適用済みの形、Schema は wire.schema の `$defs.node` へ `$ref`) で宣言する (kawaz 裁定 2026-07-31 / 2026-08-01、fable5-high と codex-sol-worker の独立 2 設計が収束した 13 点が正本)。発端の `--timerange` (1 トークン string 形と 2 トークン pair 形の両受け) は **tuple 型を足さずに** `or` + `seq` で書ける — 表現力の追加ではなく既存のトークン消費文法を type の内側から使えるようにしただけ。定義片なし = 従来どおり 1 トークン string 消費への縮退 (opt-in、**暗黙 raw-string 枝は足さない** — 1 トークン形も欲しければ枝で明示、暗黙枝は定義片が語る文法と実挙動を食い違わせ help 射影が実挙動を語れなくなる)。**展開は lowering 時の splice** (flag/config_file/tty が既に構造を注入している前例の一般化) で新しい相を作らず、位相は **A 群 (純構文正規化) の後・B 群 installer 不動点の前に走る型展開段** (registry 解決を要するので A 群ではない)。**sub-parse は独立パーサではなく、展開グラフを外側 path-search が直接歩く** — 完全経路 0/1/2+ の判定は外側 (DR-038 不変)、曖昧性解決は既存の or 枝選択/Reject/バックトラック/variable-arity がそのまま担い、positional 配置の interleave も外側機構がそのまま持つ。合流 ABI は **sealed scope の完了遷移**で枝ローカルの構造化 Value を value_parser へ渡し外側セルへ 1 set。splice は **2 位相** — descriptor 単位で memoize 可能な immutable fragment template の生成と、occurrence ごとの fresh instance 化 (scope identity / 内部セル / ref-link binding) に分かれる。**sealed scope 4 規則**: lexical chain は定義片 root で終端 (外→内・内→外とも不可視) / 内部完結の ref・link 可 / **definitions は持てない** (共有構造は registry 型へ切り出し — 型の同一性が使用文脈で変わる余地を作らない) / export_key は内部消費専用。ただし**閉じるのは名前・binding 空間であって type レジストリは継承される** (派生パーサは binding をクリアするがレジストリは引き継ぐ)。leaf の値源ラダーは「CLI 消費 > default」の 2 段 (env/config/inherit/link 席は無い)。定義片内の universal fn (default_fn 含む) は **observes が定義片内の option edge に閉じるものだけ合法** — ambient (env/system) 観測を持つ fn は `invalid-range` (「CLI だけが定義片を通る」分界の保全)、名前で外を指せば `absent-ref`。**要素間制約 4 種 (requires/conflicts_with/exclusive_group/required_group) 可** (SPL-Q1=a) で、**違反の outcome はその内部枝の Reject** (args_pos はトリガトークン) — 「外側の経路成立条件に合流しない」とは外側の constraint graph・名前解決と空間を共有しないという意味で、枝が死んで候補が減ること自体は直接歩く機構の当然の帰結。単項 `required` は sealed 内 2 段ラダーの下で既存の経路完全性意味論どおり。外部界面語彙 (long/short/env/config_key/global/alias/commands/dd/on_failure) と `definitions` は **`invalid-range`** — **inert 許容にしない** (型作者の bug を黙って埋めないため)。leaf type 未解決は `unknown-vocab` (wire の warn+string フォールバックと非対称 — 名乗る側の宣言だから)、**descriptor 内型参照は registry のみで解決** (SPL-Q3=a、DR-126 §1 と同一規則)。**`io_type.input` の string 固定 (DR-107 §7) を撤廃** — 定義片産出形・env string・config 供給形の和を宣言し、定義片から導出した産出形が宣言域外なら `invalid-range` (「宣言 vs 宣言」の定義時静的検査で、DR-126 §4 の runtime Error とは位相が違う)。**産出形は lowering 後の cell pipeline の最終 value_type から導出** (leaf = 参照 type の out / filters = io_type どおり / repeat → array / multiple → accumulator の出力 / name・export_key → kv キー / or → union / default は presence にのみ影響)、包含判定は union が成分ごと・record ⊂ map<string,value>・value は全域。供給界面は **CLI だけが定義片を通る** (env は string 1 本が value_parser へ直行、config は構造化 JSON が直行、link は出力世界で非交差 = DR-127 §5)。域外供給は供給源によらず **Reject 系** (config 供給値が input 宣言域外でも Error にしない — 供給はユーザ世界、帰属は既存の config 失敗規則で args_pos なし)、string を input に含まない型への env 席宣言は静的 dead だが **lint 警告**で definition-error にしない (vacuous required と同じ整理)。定義片 default と organic 部分書きは**無橋**。契約は **type 自身 (registry 住人) に住み**、それを指す場所が 3 箇所 (単体入口の type / 定義片 leaf の type / out record フィールドの type 参照 — 3 は link 注入時にその型のパーサを行使する = DR-127 §3.2)、record 側の型導出が見るのは out のみ。ネスト splice 可・深さ上限なし・`input_structure` 経由の循環は `circular-ref`、repeat/multiple/accumulator 可 (型依存 DAG 展開後の `min_tokens(input_structure)` が **repeat のゼロ進捗検査の入力**になり、0 消費 fragment を無制限 repeat に置けば `zero-progress`)、**greedy は配置の既存規則そのまま** (option 値スロット = 一体消費 / positional = 背骨割り込み。一律一体消費は新規則になるため不採用)。help は既存 `value_structure` へ**完全委譲** (or/seq/repeat/single/type_ref への同名写像、DR-113 §2.3 の help_category 先例の一般化、1 行合成は DR-115 §5.1 の `auto` 既定) で、**types 集約に registry 型も載せる** (id は registry 型を **canonical な ns 付き id** (`builtin/foo` 形) へ正規化し definitions ローカルは bare のまま = bare 同綴り衝突の解消、type_ref も同 identity。descriptor の description は types[].help へ写さない — 実装者向け自己記述のため)。観測面は定義片内セル = 内部セル族 (effects/result/sources 非露出)、外側セルへ 1 set (source は入口どおり)、失敗は原因トークンの args_pos に帰属、errors[].element は外側 entity、補完の候補 origin も外側 entity で内部名は漏らさない。**内部枝 identity は外側 path identity に含まれない** — 経路同一性は value_parser 適用後の外側効果列で判定し、同じ効果列に畳まれる内部枝は 1 経路へ合流 (DR-038 不変)。conformance ビークルは **2 系統併用** (SPL-Q2=a+b、役割直交): `builtin/struct` (identity の configurable factory) が splice 機構の pin とユーザ機能を担い、`fixture/*` residents が変換系 parser 挙動 (string 形正規化・部分 range・乖離 Error 近傍) を担う。`builtin/struct` の config が受ける semantic shape は `input_structure` (同じ wire 正規形) と out record 宣言で、**descriptor 軸と factory config の 2 経路が同じ定義片語彙を受ける** (綴りのみ schema 起草へ)。value_parser ABI は string でなく **Value を受ける**形へ。実装契約として template レベルは descriptor 単位で memoize 可 / path-search の memo key は外側 argv 位置・spine/greedy 状態・枝ローカル効果を含む意味同一 state / resource 上限到達は semantic な parse failure と区別して報告
- [DR-114](DR-114-universal-fn-integration.md): universal fn 統合 — variant effect / filter / default_fn の共通 DSL、1 段 array 記法、filters + cell_fns の 2 registry、統一 FnCtx + mode 判別、observes 依存軸
- [DR-118](DR-118-staged-outcome-and-ambiguous-resolve.md): 段階型契約と Ambiguous の resolve 遷移 (REV-Q3=a) — parse→ParsedOutcome / resolve→ResolvedOutcome の段階型で相区分 (DR-104 §5) を API 面でも強制、output は resolved_bindings のみ受理 (resolve 飛ばしの silent 欠落を型で封鎖)、ambiguous からの前進は `resolve_interpretation` (解釈を 1 つ選んで値源ラダー適用、選好基準は利用者の関心 — 順序源が原理的に無い DR-038 の帰結で既定解釈を置かない)、interpretation ビューは parse 相まで + 2 規則 (Default-source scalar 除外 / 空 accumulator 保持、claimants 席の規則は DR-120 で座席消滅) の規範化 = 参照実装と既存 fixture の追認で期待値変更ゼロ、遷移自体の fixture 化は解釈同定子の設計が要るため必要実体化時の DR に委ねる (射程外)
- [DR-109](DR-109-kuu-ux-skeleton-and-cli-contract.md): kuu-ux 設計骨子 (7 本柱) と kuu-cli 契約の初期裁定 (UX-Q1〜Q7) — kuu-ux = UsefulAST 実体化の 3 責務 (Definition/Export/Binding)、書き味合流モデル、$required = named capability marker + export validation、help/error は semantic model まで共通、CLI envelope は fixture expect と厳密一致 (余剰フィールド不可、解析コンテキスト露出は将来保留)、sources 常時出力、interpretations は resolve 非適用 (途中経過を規定せず最適化余地を保つ)、kuu-cli はテストツールでなくアプリ内 kuu と同一挙動 (実環境を既定取得 + --no-env/--env k=v 等の試験用オプション、極小バンドルモード・RPC クロージャ注入構想の土台)、既定 resolve 維持 + preset default は export_key 共露出に非参加 (collision.json §divergence の決着)
- [DR-110](DR-110-kuu-core-standard-packaging.md): kuu-core 標準パッケージング — 全言語実装は engine (機構のみ、builtin 語彙を含まない) / builtins (canonical 住人、公開 extension interface のみ使用 = 3rd party と差し替え可能) / assembly=kuu (組成所有・front_door 一本道の玄関・conformance の主語) の 3 層で構成 (PKG-Q1〜Q4 全て a、kawaz 2026-07-16)。依存は一方向 (assembly→builtins→engine)、engine 内蔵集合は閉じた列挙 11 項 (open node 契約・pluggable matcher・ラダー骨格・install 契約・generic 2 検査…)、boundary survey 従属争点 19 件の境界裁定表を規範化。sentinel の外部 API 露出 (is_sentinel) は廃止し玄関が吸収。conformance の検証対象は assembly (engine 単体は将来課題)
- [DR-119](DR-119-open-node-extension-abi.md): open node 拡張 ABI — node / matcher 拡張の公開契約を言語非依存に規範化 (DR-110 §3-2/§3-4 の内蔵機構規定に公開 ABI 面を与える別関心の新 DR)。設計原理 =「構築と観測を公開、分解は評価器の専権」(参照実装 resident 14 種の接触面全数調査で分解の実需ゼロ)。node 契約は必須 4 面 (kind/encode/consumes_zero_tokens/eval) + optional 3 面、branch 3 値 union (accept/held/pending) が安定面 (variant 追加 = major)、resume は現 eval 呼び出しの継続のみで保持禁止、ctx 観測 3 面 (token_at/is_complete_mode/extensions)、消費構造の構築面は structural 4 種 (exact/bind/seq/or) + ext_node 注入 (scope/ref/反復骨格は需要実体化時に minor 追加)、評価プロトコル型は公式構築子のみ (内部座標は評価器が付与)、consumes_zero_tokens は宣言義務 (虚偽宣言は未定義・検証は lint の関心)、matcher も同プロトコル共有。DR-107 role enum に node/matcher 追加。plant DSL 中間層・ADT 公開・実行時実測検証を不採用
- [DR-121](DR-121-sources-result-address.md): sources の結果アドレス (SRCADDR-Q1=d / Q2-α=a / Q2-β=c) — flat map (`{"sub.ttl":"cli"}`) をやめ structured path の配列 (`[{path:["sub"],key:"ttl",source:"cli"}]`) へ。`.` 連結が非単射で、`export_key:"a.b"` の leaf と scope `a` 配下の `b` が同居すると result は 2 セルを区別するのに sources は 1 エントリしか持てない (DR-120 上合法な定義なので definition-error でも弾けない)。符号化を決めるのでなく不要にする方を採用 (escaping / encoding は消費者に復号を要求し、忘れた実装が静かに壊れる)。席を持つのは値源ラダーで確定した値セルのみ — presence marker / 透過セル / 内部セル (none・dd。**config_file は DR-135 で内部セルから外れ通常要素になった**) は持たず、0 回発火 accumulator は `[]` の席として default を持つ。structural aggregate は単一値 (or の枝 = union 席) なら entry 1 件、複数値 (seq = tuple、席が N 個) なら要素ごと。異なる値源の共存は 0 消費 literal (`value:`) で到達可能なため 1 タグ化は誤報になる。effects は id 軸で不変 (DR-045、綴りは TRG-Q3=a で id へ確定)。`link` は `cli` に畳まず独立タグ (LINKSRC-Q1=a) — alias の deprecated ペアのように「どちらから入っても結果は同じだが deprecated な入口を使ったら警告したい」用途で経路の判別が要る。参照実装は Cli に畳んでいるので追随が要る — **§1 / §2.2 / §3 は DR-122 が置き換え** (§4 / §5 は現役)
- [DR-122](DR-122-sources-shadow-tree.md): sources は result の shadow tree — entry 配列をやめ、`result` と同じ構造で値の座だけを source タグに置き換えた形にする (`result: {"pair":["x","fallback"]}` → `sources: {"pair":["cli","const"]}`)。DR-121 が `.` 連結の非単射を潰すために発明した structured path entry (フラット化 + アドレス語彙) は、そもそもフラット化しなければ不要になる中間発明だった (kawaz 裁定 2026-07-26:「シンプルに値の構造そのままで何でダメなの、pair[0] の source が cli って方が正確」)。キー集合は `result` の射影 — result に現れないキーは sources にも現れない (未選択 scope / absent は成立しなかった構造であり source を語る対象が無い、「result に出てこないのは死んだ枝」)。タグの決定単位は「値の座」で、nameless tuple の各要素・accumulator の各要素がそれぞれ自分の由来を持つ (DR-121 §3.2 の「wrapper に 1 タグ」はフラット化由来の縮退表示だった、merge accumulator が下位席の old を splice すれば `["env","env","cli"]` の混在も書ける)。空コレクション `[]` / `{}` は値の座が無いのでタグを持たず、0 発火 accum に default entry を捏造する DR-121 §2.2 は廃止 — 代償として `empty` op (committed=true) の空と未発火の空が sources 上で同形になり、この区別は effects の op が担う。比較規約は `result` と同じ構造等価で足り (kv は unordered / 配列は要素対応)、順序の論点は構造が持ち込むので sources 固有の順序規約を持たない
